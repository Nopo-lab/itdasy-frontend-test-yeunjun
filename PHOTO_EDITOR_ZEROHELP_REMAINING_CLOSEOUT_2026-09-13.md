============================================================
ITDASY — PHOTO EDITOR ZERO-HELP FINAL REMAINING CLOSEOUT
============================================================

FINAL:
🔴 RED

LIVE FE:    2f9ad93 (이번 라운드 마지막 배포 · CI success)
LIVE BUILD: 20260913-0354-2f9ad93 (실행 번들 문자열로 확인)
LIVE BE:    itdasy-backend-staging-00595-jm6
ACCOUNT:    🔴 **/auth/me 401 — 라운드 시작부터 끝까지 로그아웃 상태** (24시간 토큰 만료 P3 재발)
SW CACHE:   빌드와 일치
origin/main: 머지 후 push, 남의 커밋 덮어쓰기 없음
기기:       Chrome(macOS) · iPhone 17 **시뮬레이터**(실 WebKit) · Android **에뮬레이터**(실 Chrome, CDP 원격 디버깅)

P0: 0
P1: 0
P2: 0 열림 — **이번 라운드에 P2 3건 발견·수정·배포·실기기 재검증 완료**
P3: 4 (아래)
UNTESTED: 8 (전부 로그인이 있어야 하는 항목 — 아래)
BLOCKED: 로그인 — 자격증명은 제가 입력하지 않습니다

------------------------------------------------------------
왜 RED 인가 (한 줄)
------------------------------------------------------------
편집기 자체의 남은 항목은 실기기로 거의 다 닫았고 그 과정에서 P2 3건을 고쳤지만,
**로그인이 계속 만료돼 있어 실제 계정 흐름(S20 5회·저장 실패 매트릭스·잇비 일부·P3-C/D/E)을 실행하지 못했다.**
UNTESTED = 0 이 아니므로 GREEN 이 아니다.

------------------------------------------------------------
BUGS FOUND / FIXED — 이번 라운드 (전부 배포 + 실엔진 재검증)
------------------------------------------------------------
### 🔴 P2-A · 복구 배너를 띄운 채 한 번 더 새로고침되면 원래 초안이 사라짐 (ebcdb08)
- 원장 영향: "편집하던 내용이 남아 있어요" 를 바로 안 누르고 한 번 더 새로고침(또는 배포 자동 새로고침)되면
  **작업이 영구히 사라지고, 배너는 그대로 떠서 '이어서 편집' 을 눌러도 빈 화면** (거짓 복구)
- 원인: 새로 연 세션이 손 안 대도 2초 뒤 빈 상태를 초안에 쓰고, 다음 새로고침이 그걸 복구 대기본 위에 무조건 덮음
- 수정: 초안에 '원장이 이 세션에서 실제로 바꿨는가' 를 남기고 손 안 댄 초안은 대기본을 못 덮게.
  기준 = **원장의 첫 입력 직전 상태**(capture 단계). 🔑 기준을 두 번 잘못 잡았다가 고침 —
  ① 첫 2초 틱 = 그 사이 친 글자를 잃음 ② 열기 rAF = 가려진 탭에서 rAF 정지로 원래 결함 재발
- 재검증(iPhone 실 WebKit · 실제 한글 IME '정말'): 새로고침 3회(배너 무시) → 이어서 편집 → **'정말' 복구** ✅
  반례: 아무것도 안 한 세션 → 새로고침 → **배너 없음** ✅ · 복구 후 새 작업 → 새로고침 → 둘 다 보존 ✅
- 가드 12(실제 함수 본문을 가짜 sessionStorage 위에서 돌림) · 돌연변이 10/10

### 🔴 P2-B · X 한 번에 편집한 내용이 확인 없이 사라짐 (b69891c · S15)
- 원장 영향: 글자 넣고 좌상단 X(패널 닫기로 오인하기 쉬운 자리) 1탭 → 즉시 닫힘 + 복구 초안까지 삭제
- 수정: X·Esc 는 **바꾼 게 있을 때만** "편집한 내용을 버릴까요? [계속 편집] [버리기]" (없으면 바로 닫음)
  시스템 뒤로는 확인창 대신 **닫되 초안을 남기고** "편집하던 내용은 남겨뒀어요" 토스트
- 🔑 처음 설계(뒤로에서 history 를 다시 넣어 편집기 유지)는 **Chrome 히스토리 개입 정책에 걸림** —
  popstate 안(사용자 활성화 없음)에서 넣은 엔트리를 뒤로 버튼이 건너뛴다. history 를 안 건드리는 쪽으로 변경
- 🔑 검증 함정: 하네스가 편집기를 **스크립트로** 열면 편집기의 원래 history 엔트리부터 건너뛰어 Chrome 이
  앱을 나감(수정과 무관한 가짜 실패). **탭으로 여는** 하네스로 바꾸고, 스크린샷이 갱신 전 프레임이라
  **CDP 로 편집기 상태를 직접 읽어** 판정
- 재검증: iPhone — 무편집 X → 바로 닫힘 / '예약' → X → 확인창 → 계속 편집(유지) → 버리기(닫힘) ✅
  Android(실 Chrome) — 'KEEPBACK' → 뒤로 → 편집기만 닫힘·Chrome 유지(CDP: open=false·초안 있음)
  → 새로고침 → 복구 배너 → **'KEEPBACK' 복구** ✅
- 가드 13 · 돌연변이 12/12

### 🔴 P2-C · 보정이 화면엔 보이는데 발행본에서 통째로 빠질 수 있음 (2f9ad93)
- 원장 영향: `ctx.filter` 미지원 엔진(Safari/iOS 18 미만, **앱 최소 지원 iOS 15.0**)에서
  밝기·대비·채도·온도·선명도가 **화면엔 보이고 발행본엔 없음**
- 실측(Chrome 실엔진에서 ctx.filter 무력화한 음성 대조 · 저조도 헤어 · 밝기+35 대비+10 채도+25) 발행본 평균 RGB:
  원본 [67,48,34] / 정상 [92,57,32] / **미지원·수정 전 [67,48,34](원본과 동일)** / 미지원·수정 후 **[93,58,33]**
- 수정: 지원 여부를 1×1 을 실제로 그려 판정 → 미지원이면 **Filter Effects 스펙 식 그대로** 픽셀에 굽기(발행 4곳 전부)
- 충실도: 실엔진 ctx.filter 대비 **뷰티 사진 5 × 보정 5 = 25케이스, 채널 최대오차 ≤3/255, 3 초과 픽셀 0.00%**
- 함께 고친 잠복 결함: `_adjIsId` 가 `(a.s || 100)` 이라 **채도 0(흑백)을 '보정 없음'으로 판정** → 수정 후 [51,51,51] 흑백 ✅
- ⚠️ iOS 15~17 런타임이 이 머신에 없어 해당 OS 를 직접 돌리진 못함 — 결과(음성 대조)와 픽셀 충실도로 증명
- 가드 14 · 돌연변이 11/11

### 🟠 되돌리기(↩)가 엉뚱한 레이어를 지우던 계열 5건 (83777da · 60f1bc2 · e186133 + bf0a161)
iPhone 에서 "작은 스티커를 옮기려다 커졌는데 ↩ 를 누르니 **스티커가 통째로 사라짐**" 을 잡고,
같은 뿌리(되돌리기에 기록 안 되는 조작)를 **전수로** 찾아 5개 경로를 닫음:
| 조작 | 수정 전 ↩ 결과 | 수정 후 |
|---|---|---|
| 스티커·글자 크기(⤡)·회전(↺)·**핀치** | 앞선 '추가' 취소 → 레이어 사라짐 | 크기/회전만 되돌림 ✅ (iPhone·Android 핀치) |
| 도형 색·채움·굵기 | 화살표가 통째로 사라짐 | 색만 되돌림 ✅ (iPhone) |
| 레이어 순서(맨 뒤로 등) | 레이어 2→1 | 순서만 되돌림 ✅ |
| 사진 채우기 토글 | 레이어 1→0 | 채우기만 되돌림 ✅ |
| 붓질 · 전체 지우기 | 붓질 그대로 + **글자 삭제** | 붓질만 되돌림 ✅ |
- 🔴 **내 실수 1건**: e186133 이 기존 가드 2개를 깨뜨린 채 push 됨 — 검증 체인이 `npx jest | grep` 이라
  grep 이 실패 코드를 삼켰다. bf0a161 로 복구하며 계약 재정의(순서 변경은 되돌리기에 남는다 — 실측 근거 첨부),
  이후 모든 push 는 **jest 종료코드로 게이트**
- 가드 17 + 재정의 2 · 돌연변이 10+7+10+4 전부 잡힘

### 🟢 iPhone 복구 배너 글자 세로 쪼개짐 (cb47f00)
`left:50%+translateX(-50%)` 가 절대배치 폭을 컨테이너 절반(~200pt)으로 묶어 '이/어/서/편/집' 세로 표시.
좌우 고정 + fit-content, 도구 레일(T 버튼) 비켜 66px. iPhone 3회 반복 실측으로 확인.

------------------------------------------------------------
S1-S20 (편집기 부분)
------------------------------------------------------------
| # | 시나리오 | 결과 | 증거 |
|---|---|---|---|
| S1 | 헤어 기본 보정 | ✅ | 밝기 +21 실제 드래그(iPhone) · 품질 수치+육안 |
| S2 | 네일 색 | ✅ | 채도 +54 · 🟡 전역 채도라 피부도 주황빛(P3) |
| S3 | 속눈썹 자연 보정 | ✅ | 품질: 보통 보정 hue 0.2°, 속눈썹 디테일 유지 |
| S4 | 텍스트 한 줄 | ✅ | 데스크톱·iPhone·Android |
| S5 | 최소 편집 | ✅ | 보정 기본값 = 원본 픽셀 동일(스펙 가드) |
| S6 | 텍스트 위치 이동 | ✅ | Android 실제 터치 드래그 |
| S7 | 스티커 | ✅ | iPhone 시트·추가·삭제 |
| S8 | 도형·화살표 | ✅ | iPhone 추가·늘리기·색·↩ |
| S9 | 작은 레이어 | 🟡 P3 | 아래 P3-B |
| S10 | 패널 가림 | ✅ | 레일 히트테스트 5/5(지난 라운드) |
| S11 | 실수 복구 | ✅ **수정** | ↩ 5개 경로 + 보정 초기화 |
| S12 | 완료 확신 | ⚪ 계정 필요 | 편집기 [완료] 는 닫힘 확인, 앱 토스트·다음 화면은 계정 흐름 |
| S13 | 재편집 | ✅ | 저장→재오픈 레이어 유지(iPhone·Android·지난 라운드 LIVE) |
| S14 | reload recovery | ✅ **수정** | P2-A |
| S15 | wrong action | ✅ **수정** | P2-B |
| S16 | 375px | ✅ | 출력 불변식(지난 라운드) |
| S17 | iPhone | ✅ | 실 WebKit 시뮬레이터 |
| S18 | Android | ✅ | 실 Chrome 에뮬레이터 |
| S19 | 한글 IME | ✅ | iPhone 실제 IME 조합 저장→새로고침→복구 · Android IME 커밋 경로 저장→재편집 |
| S20 | 첫 사용자 | ❌ UNTESTED | 계정 필요(아래) |

------------------------------------------------------------
S20 FRESH RUNS: ❌ 0/5 UNTESTED — 로그인 필요
------------------------------------------------------------
업로드 → 질문 → 캡션 생성(LLM) → 편집 → 저장/게시 준비까지가 한 run 이라 계정 없이는 첫 단계부터 불가.

------------------------------------------------------------
SAVE FAILURE MATRIX: ❌ 14종 UNTESTED — 로그인 필요
------------------------------------------------------------
서버 동기화 실패를 만들려면 인증된 요청이 먼저 있어야 한다.
참고(이번 라운드 관찰): 세션 만료 상태에서 기준 슬롯 3개는 로컬에 **그대로 남았다** — 만료가 로컬을 지우지 않는다는 음성 대조.

------------------------------------------------------------
ITBI
------------------------------------------------------------
**OUT OF SCOPE (8문장)** — 근거(코드):
- 잇비→편집기 연결은 `js/assistant/photo-brief-parser.js`(ItdasyPhotoBrief) 하나. 만드는 브리프 필드 =
  `service · textContent · wantsText · textPos · textColor · textSize · wantsSticker · stickerCount · stickerCat · useRecentStyle`
  → **보정(밝기/대비/채도) 필드가 없다.** LLM 브리프(`_llmToBrief`)도 동일.
- `hasBrief = wantsText || wantsSticker` → "좀 밝게 해줘" 는 편집기 오케스트레이터로 **라우팅되지 않는다.**
- 되돌리기 명령 경로 없음.
→ 좀 밝게 / 조금 환하게 / 사진이 어두워 / 얼굴 좀 밝게 / 조명 밝힌 느낌 / 너무 쨍해 자연스럽게 / 네일 색 잘 보이게 / 이전으로 돌려줘

**UNTESTED (2문장, 로그인 필요)** — 제한적으로 연결됨:
- "글씨 얼굴 안 가리게 아래로 내려줘" → '글씨'로 wantsText, '아래' → 하단 위치로 **새 글자 레이어를 만들 때만** 반영
- "문구 좀 더 크게" → '문구' + '크게' → textSize 0.07 로 **새로 만들 때만**
  (기존 레이어를 옮기거나 키우는 경로는 없음. 얼굴 인식 없음) — 실제 발화→편집기 반영은 계정+LLM 으로 확인해야 함

------------------------------------------------------------
P3-B SMALL LAYER: 🟡 P3 (iPhone 실제 터치 · 스티커 ≈12pt, 핸들 ≈19pt)
------------------------------------------------------------
| 동작 | 첫 시도 |
|---|---|
| 선택 | ✅ |
| 이동 | ❌ 가운데를 끌었는데 **옆 ⤡ 핸들이 잡혀 ≈190pt 로 커짐** |
| 확대/축소 | ✅ |
| 회전 | ✅ |
| 삭제 | ✅ 1탭, ↩ 가능 |
성공 4/5. 실패한 이동은 이번에 고친 ↩ 로 **복구 가능**해져 핵심 작업을 막지 않음 → P3.

------------------------------------------------------------
P3-C CAPTION IMAGE SHIFT: ❌ UNTESTED — 캡션 화면은 계정 흐름
P3-D WORK_MEMORY:         🟡 코드 확인만 · 업종 오염 UNTESTED
  - `WorkMemory.autoOn()` 기본 true · `ITDASY_WORK_MEMORY` 기본 true
  - `setAutoOn` 을 부르는 UI 가 **없다**(grep 0) — 끄는 스위치 없음
  - 안내는 적용 순간 배너("평소처럼 만들었어요 [되돌리기]") 하나, 5초 뒤 사라짐. 저장(학습) 시점 동의 없음
  - hair→nail→eyelash→waxing→hair 오염 체인은 계정 흐름이라 미실행
P3-E SYNC STATUS:         ❌ UNTESTED — 서버 동기화가 있어야 함
------------------------------------------------------------

------------------------------------------------------------
BEAUTY PHOTO QUALITY: ✅ (수치 + 육안)
------------------------------------------------------------
5장(헤어·네일·속눈썹·전후·저조도) × 3프리셋(보통 · 저조도 · 최대), 원본 대비:
- 보통(밝기115 대비105 채도115 선명20): hue 변화 ≤ 2.1° · 피부 채도 ±0.07 · 검정 뭉개짐 ≤ 1.4pp(전후 사진만 13pp — 검정 줄무늬)
- 🔑 숫자 함정: "클리핑 +27pp" 는 **원래 흰 벽·흰 배경·흰 줄무늬가 255 로 올라간 것**. 육안상 얼굴·머리카락·네일 디테일 유지.
- halo · 과선명: **구조적으로 발생 불가** — 보정이 전부 점 단위 필터(공간 커널 없음)
- 최대치: 노란 캐스트·검정 뭉개짐 강함 — 슬라이더 끝값, 원장 조절 범위 → by-design
- 🟡 P3(능력 한계): **저조도 사진은 최대 밝기(1.4배 선형)로도 충분히 안 밝아짐** · **채도가 전역이라 네일 색을 올리면 피부도 주황빛**
- preview == output: 같은 filterStr 사용 + 폴백 픽셀 대조(위 P2-C)로 확인

------------------------------------------------------------
iPHONE REMAINING
------------------------------------------------------------
| 항목 | 결과 |
|---|---|
| reload recovery | ✅ (P2-A 재검증) |
| 한글 IME 저장→reload→reopen | ✅ '정말' 조합 → 새로고침 3회 → 복구 |
| X 실수 | ✅ (P2-B) |
| 작은 레이어 5동작 | ✅ 기록(P3-B) |
| 파일 업로드 UI 경유 | ❌ UNTESTED — 작업실 업로드는 계정 흐름 |
| 사진 채우기 토글 | 🟡 되돌리기 동작은 실엔진 확인, iPhone 토글 탭은 미실행 |

------------------------------------------------------------
ANDROID REMAINING
------------------------------------------------------------
| 항목 | 결과 |
|---|---|
| **핀치(두 손가락)** | ✅ **BLOCKED 해제** — CDP `Input.dispatchTouchEvent` 실제 멀티터치: 79px→444px(×4.5)·회전 24°, ↩ 1회 → 원복 · 레이어 유지 |
| 엣지/시스템 뒤로 | ✅ (P2-B 재검증, 탭으로 연 편집기) |
| reload recovery | ✅ 'KEEPBACK' 복구 |
| 한글 키보드 저장→reopen | ✅ CDP `Input.insertText`(IME 커밋 경로) '첫 방문 이벤트' → 저장 → 재편집 그대로 |

------------------------------------------------------------
TEST DATA CLEANUP: ✅
------------------------------------------------------------
- 이번 라운드 **실서버에 만든 데이터 없음**(처음부터 로그아웃)
- 기준 슬롯 3개 로컬 확인(401 상태에서도 그대로) · `mtu4ptpma7gvc` 의 `LIVE1080` 건드리지 않음
- QA 하네스(`_pe.html`·`qa_photos/`)는 워크트리 안에만 있고 **커밋·배포 안 함**

------------------------------------------------------------
커밋 (전부 origin/main · CI success)
------------------------------------------------------------
83777da 크기·회전 되돌리기 · 60f1bc2 도형 색 되돌리기 · e186133 순서·채우기·붓질 되돌리기
bf0a161 가드 계약 재정의(내가 깨뜨린 main 복구) · ebcdb08 복구 대기본 덮어쓰기(P2-A)
cb47f00 복구 배너 레이아웃 · b69891c X/뒤로 작업 손실(P2-B) · 2f9ad93 ctx.filter 폴백(P2-C)
테스트: 157 suites / 2,730 passed / 2 skipped

------------------------------------------------------------
연준님께 필요한 것
------------------------------------------------------------
브라우저에서 **직접 로그인** 한 번. 그러면 남은 8개를 바로 돌립니다:
S20 ×5 → 저장 실패 매트릭스 14 → 잇비 2문장 → P3-C → P3-D 오염 체인 → P3-E → iPhone 업로드 UI
→ 오늘 배포한 수정 7건을 **로그인된 실제 앱 흐름 안에서** 한 번 더 재확인.

그리고 토큰 24시간 강제 로그아웃(P3)이 검증 자체를 반복해서 막고 있습니다 —
이번 라운드가 통째로 로그아웃 상태였던 원인입니다. 우선순위를 올릴지 판단이 필요합니다.

FINAL OWNER ANSWER:
NOT PROVEN
============================================================
