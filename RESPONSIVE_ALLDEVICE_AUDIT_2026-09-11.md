# ITDASY — ALL DEVICE / ALL SCREEN RESPONSIVE AUDIT

**DATE**: 2026-09-11
**BRANCH**: `feat/responsive-alldevice` (워크트리 `../itdasy-wt-responsive`, base `342002c`)
**FE 검증 대상**: 로컬 정적 서버로 서빙한 **워크트리 실파일** (배포본 아님)
**BE**: 목(mock) — `window.fetch` 가로채기. 실백엔드 아님(사유: staging 이 localhost Origin 에 CORS 를 안 연다)

---

## 0. 이 라운드가 앞 라운드와 다른 점

2026-09-07·09-08 라운드는 **홈 화면 + 시트 4개**를 iOS 시뮬레이터/안드 에뮬레이터에서
깊게 봤다(키보드·시스템백·safe-area). 이번 라운드는 **넓이**를 맡았다 —
화면 15종 × 폭 320~3440px × 세로/가로 × dpr 1/1.25/2/3 을 **자동 순회**하고,
매 지점에서 레이아웃·가림·터치타깃을 계측했다.

**계측 방식(추정 아님)**
- 실제 Chrome(헤드리스 신모드) + Puppeteer. 뷰포트·dpr·isMobile·hasTouch 전부 에뮬레이션.
- 화면이 **실제로 열렸는지 매번 확인**(`opened` 플래그). 안 열렸으면 최대 3회 재시도하고,
  그래도 안 열리면 그 레코드는 PASS 로 세지 않는다.
  → 첫 시도에서 이 가드가 없었다면 **32기기 중 24기기가 '안 열린 화면'을 PASS 로 셀 뻔**했다
    (헤드리스에서 포그라운드가 아닌 페이지는 CSS 애니메이션이 `opacity:0` 에서 멈춘다).
- 목 데이터는 **긴 한글 이름·긴 메모·예약 40건(하루 10건)·고객 40명**으로 넣어
  빈 화면이 아니라 **꽉 찬 화면**을 쟀다.

---

## 1. 커버리지

### 1-A. 폭 촘촘 스윕 (breakpoint 사이 공격)

화면 15종 + 드로어 = 16종 × 3개 밴드(모바일360/태블릿900/PC1280 앵커) × 폭 60지점
= **960 계측 지점**, 수정 전/후 각각 1회.

| 밴드 | 앵커에서 화면을 연 뒤 리사이즈로 훑은 폭 |
|---|---|
| 모바일 | 320 330 344 352 360 368 375 384 390 393 402 412 420 430 440 450 460 480 500 540 560 585 600 620 640 660 680 700 720 744 760 767 |
| 태블릿 | 768 780 800 820 834 860 900 960 1000 1024 1060 1099 |
| PC | 1100 1152 1180 1199 1200 1280 1366 1440 1536 1600 1728 1920 2200 2560 3000 3440 |

### 1-B. 실기기 뷰포트 매트릭스

| Device Class | Viewports | Portrait | Landscape |
|---|---|---|---|
| iPhone | 320×568 · 375×667 · 390×844 · 402×874 · 430×932 | PASS | PASS (844×390 · 932×430) |
| Galaxy / Android | 344×882(Fold접힘) · 360×740 · 412×915 · 480×800 · 585×820(Fold펼침) | PASS | PASS (740×360 · 915×412 · 800×480) |
| Android Tablet | 600×960 · 768×1024 · 1280×800 | PASS | PASS (1024×768) |
| iPad | 834×1194 · 1024×1366 | PASS | PASS (1194×834) |
| Notebook | 1024×768 · 1280×800 | PASS | — |
| Desktop | 1920×1080 | PASS | — |
| Ultrawide | 3440×1440 | PASS | — |

### 1-C. 화면 15종 (전부 '열렸는지' 확인하고 감사)

홈 · 내 샵 관리 · 예약관리(월/주) · 고객관리 · 매출관리 · 인스타DM · 인스타댓글 ·
연결된 서비스 · 샵 관리(설정 허브) · 설정 시트 · 이용 플랜 · 잇비 챗 · 작업실 ·
고객센터 · 회원권 만료 + **사진편집기(ItdEditor)** 별도 14뷰포트

---

## 2. 찾은 것 / 고친 것

### BUG-R1 (P1) — 안드로이드 폰을 가로로 눕히면 데스크톱 UI 가 뜬다

```
Viewport : 844×390 (iPhone 12 가로) · 915×412 (Galaxy S24 가로) · 932×430 · 800×480
Route    : 전 화면
Observed : 사이드바(232px) + PC 헤더 + 데스크톱 여백이 390px 높이 안에 전부 들어와
           예약 달력은 첫 줄(y≈525)조차 화면 밖. 하단 탭바는 PC 규칙이 숨겨서 없음.
Expected : 폰 크기 화면이면 세로든 가로든 모바일 셸
Root     : PC 셸 조건이 **폭만** 본다 — `@media (width >= 768px)`.
           폰 가로는 폭이 768 을 넘는다.
           · iOS 는 Info.plist 가 아이폰을 세로로 잠가서(iPad 만 가로 허용) 안전했다
           · 🔴 **AndroidManifest.xml 에 `screenOrientation` 이 없다** → 갤럭시는 그대로 돌아간다
           · 모바일 웹(설치 안 한 브라우저)은 iOS/안드 둘 다 돌아간다
Fix      : PC 셸 조건에 높이를 추가 — `(width >= 768px) and (height >= 600px)`.
           사이드바 오프셋을 쓰는 블록 9곳에 동일 적용
           (style-responsive / style-components×2 / myshop-v3 / itd-editor /
            workspace-v2 / workspace-crop / workspace-v2-flow / sub-screens)
           모바일 전용 블록은 `(width <= 767px), (height < 600px)` 로 대칭 확장.
Verify   : 844×390 · 915×412 · 932×430 · 820×585 → sideNav `display:none`,
           `body padding-left: 0`, 탭바 `grid`, 잇비 FAB `flex`
           960×600 · 1024×768(아이패드·안드탭 가로) → sideNav 232px 유지, 탭바 숨김 (변화 없음)
Status   : FIXED
```

**왜 600px 인가**: 아이패드 가로(768) · 안드탭 가로(800) · 노트북(768+) 은 전부 PC 를 유지하고,
폰 가로(360~480) 만 모바일로 돌아오는 유일한 경계선이다. 585(Fold 펼침 가로)는 모바일로 간다 — 의도한 것이다.

### BUG-R2 (P1) — 작업실 필터 줄이 320~344px 에서 페이지를 밀어냈다

```
Viewport : 320(30px 초과) · 330(20px) · 344(6px) — 350px 이상에선 정상
Route    : 작업실(홈 피드)
Observed : documentElement.scrollWidth 350 vs clientWidth 320 → 가로 스크롤
Root     : `.wf-segs` 가 flex nowrap 인데 pill 3개(전체/진행중/발행됨) + '성과' 가
           전부 `white-space:nowrap`. 320px 에선 350px 가 필요했다.
Fix      : 필터 3개만 가로 스크롤 래퍼(`.wf-segs__scroll`)로 접고 '성과'는 오른쪽 고정.
           (줄 전체를 스크롤로 만들면 '성과'가 영영 화면 밖으로 나간다)
Verify   : 320/330/344/360 전부 docOverflow 0. 960 지점 재스윕에서 가로 넘침 0.
Status   : FIXED
```

### BUG-R3 (P2) — 고객관리 가나다 인덱스바: 9px 글자 · 15×16px · 검색창과 겹침

```
Viewport : 전 모바일 폭
Observed : 우측 자음 인덱스가 `top:200px` 고정이라 검색창 세로 구간과 겹쳤고,
           한 칸이 15×16px 였다(자음 14개면 조준이 사실상 불가).
Root     : `top:200px` 은 헤더가 한 줄일 때만 맞는 값. 샵 이름이 길거나 320px 에서
           통계 카드가 접히면 검색창 위로 올라온다.
Fix      : ① 시작 높이를 JS 가 칩 줄 아래로 실측해 넣는다(app-customer.js)
           ② 남는 높이를 자음 개수로 나눠 갖게 + 열 폭 26px 전체를 히트영역으로
Verify   : 320×568 → 칸 25×26px (기존 15×16) · 390×844 → 34×26px
           인덱스바 top 265 > 검색창 bottom 209 (겹침 해소)
           검색창 오른쪽 끝 탭 가능 여부 = true
Status   : FIXED
```

### BUG-R4 (P2) — 손가락 영역 44px 미달 (전 화면 실측)

수정 전 전 화면 스윕에서 21종. 이웃 간격(gL/gR/gT/gB)을 먼저 재고
**넓혀도 옆 버튼의 중심을 안 뺏는 것만** 키웠다. 보이는 크기는 안 건드렸다(`::after`).

| 컨트롤 | 화면 | 전 | 후(실측 히트박스) |
|---|---|---|---|
| `button.ss-close` (공통 닫기) | 연동관리·회원권 외 | 18×18 | **44×44** |
| `.rv-header__month .ar` ‹ › | 매출 월 이동 | 26×26 | **44×44** |
| `.crq-gear` | 인스타 댓글 설정 | 27×27 | **44×44** |
| `#shHaptic` | 설정 진동 토글 | 36×22 | **44×44** |
| `.wshc-more` | 작업실 더보기 | 34×34 | **44×44** |
| `#assistantSheetHeader button` ✕ / ⋯ | 잇비 | 32×32 | **44×44** |
| `#asstPhoto` / `#asstMicBtn` | 잇비 사진·음성 | 40×40 | **44×44** |
| `#supportChatCard .ss-close` | 고객센터 | 40×40 | **44×44** |
| `.ms-ic__seg-btn` 보통/크게 | 설정 글자크기 | 39×23 | **≥49×44** (세로만) |
| `.dmm-tg` 켜기 | DM 메뉴 토글 | 44×26 | **44×40** (아래 여유 7px 한계) |
| `[data-report-ai]` 신고 | 잇비 | 41×17 | **42×40** (아래 여유 12px 한계) |
| `.dmm-addbtn` + 메뉴 추가 | DM 메뉴 | 340×43 | **44 높이** |

**일부러 안 고친 것 (기존 14-b 주석의 판단을 유지)**
`.bk-view__btn`(월/주 30×44) · `.bk-today-btn`(37×44) — 세그먼트라 좌우가 붙어 있다.
넓히면 옆 버튼을 뺏는다. **못 누르는 것보다 엉뚱한 걸 누르는 게 더 나쁘다.** 세로는 이미 44.

⚠️ `.dmm-tg` 는 **`::after` 를 노브로 이미 쓰고 있었다** — 런타임 `<style>` 주입이라 CSS 파일
grep 으로는 안 보인다. computed 로 잡아서 이것만 `::before` 를 썼다.

### BUG-R5 (P2) — 울트라와이드에서 내용이 끝없이 늘어난다

```
Viewport : 2560×1440 · 3440×1440
Observed : 내 샵 관리 본문이 사이드바 뺀 3,180px 를 통째로 채워, 도넛 차트 하나에
           1,100px 카드가 붙고 나머지는 빈 공간
Root     : `#tab-dashboard.tab { max-width: none !important }` (myshop-v3.css) — ≥1100px
           2단 레이아웃이 좁아지지 않게 하려던 의도. 그 의도는 유효하지만 상한이 없었다.
Fix      : **2100px 이상에서만** 상한 — 본문 1800px / 허브 카드 1400px, 가운데 정렬.
           1920 이하 화면은 값이 하나도 안 바뀐다.
Status   : FIXED
```

### 결함 아님으로 판정한 것

| 관측 | 판정 | 근거 |
|---|---|---|
| 좌측 드로어(`#shopDrawer`)가 ≥768px 에서 사이드바에 완전히 가려 8개 메뉴 전부 클릭 불가 | **죽은 UI** — 반응형 결함 아님 | 트리거 `[data-drawer-trigger]` 가 `display:none` 인 호환용 헤더 안에만 있고, `openShopDrawer()` 호출자가 **0건**. 실사용 경로가 없다. → 정리 대상(P3) |
| 예약 달력 마지막 주가 안 보인다(첫 계측) | **오탐 — 내 계측이 틀렸다** | 스크롤 컨테이너가 `#bk-body`(overflow:hidden)가 아니라 `.bk-month-m`(overflow-y:auto) 였다. 올바른 컨테이너로 다시 재니 320×568 · 768×1024 · 844×390 · 1280×800 **전부 마지막 칸 도달 + hit-test 성공** |
| 매출 `›`(다음 달) 중심이 안 눌린다 | **정상** | `pointer-events:none` = 현재 달보다 앞으로 못 간다. 수정 전 배포본과 동일함을 같은 프로브로 대조 확인 |
| DM 토글 일부가 안 눌린다 | **정상** | 상단 마스터 토글 OFF → 하위가 `dmm-dim`(pointer-events:none). 수정 전과 동일 |
| 작업실 필터 3번째 pill 중심이 '가려짐'으로 잡힘 | **계측 한계** | 가로 스크롤러 안에서 잘린 칩이라 스크롤하면 나온다. 감사 규칙에 예외 추가 |

---

## 3. 재검증 (수정 후)

| 검증 | 규모 | 결과 |
|---|---|---|
| 폭 촘촘 스윕 (320~3440, 60지점 × 16화면) | 960 지점 | **가로 넘침 0 · 텍스트 하드 클리핑 0** |
| 기기 매트릭스 세로 | 18기기 × 15화면 = **270** | **레이아웃 신호 0 · fatal 0 · 안 열린 화면 0** |
| 기기 매트릭스 가로 | 8기기 × 15화면 = **120** | **레이아웃 신호 0 · fatal 0 · 안 열린 화면 0** |
| 사진편집기 | 14 뷰포트(320~3440 · 가로 포함) | **넘침 0 · 가림 0 · 44px 미달 0** |
| 히트박스 정밀 실측 | 수정 대상 21종 | 위 표 |
| jest 회귀 | 81 suites | **1611 / 1611 PASS** |
| stylelint / eslint | 변경 파일 전체 | 0 |
| pre-commit smoke (`--git`) | 91 scripts · 182 lazy | PASS |

> **레이아웃 신호 = 0** 의 정의: 390개 레코드 전부에서 (a) 페이지 가로 넘침 0px,
> (b) 뷰포트 밖으로 튀어나온 요소 0개, (c) 같은 패널 안에서 가려져 중심이 안 눌리는
> 조작 요소 0개. 매 레코드마다 **해당 화면이 실제로 열린 것을 확인한 뒤** 잰 값이다.

**재검증 후 남은 44px 미달 (16종, 전부 사유 확인됨)**

| 항목 | 사유 |
|---|---|
| `.dmm-tg` 켜기 · `.dmm-addbtn` | 대부분 상단 마스터 토글 OFF 상태의 `dmm-dim`(`pointer-events:none`) — 원래 안 눌리는 게 맞다. 활성 토글은 **44×40** 으로 실측 확인(아래 여유 7px 가 물리적 상한) |
| `revenue .ar ›` | 다음 달로 못 가는 비활성 상태(`pointer-events:none`). 수정 전 배포본과 동일함을 같은 프로브로 대조 |
| `[data-report-ai]` 신고 | **42×40** 실측(아래 여유 12px 상한). 44 에는 2px 모자라지만 17px → 40px |
| `.ms-ic__seg-btn` 보통/크게 | 세로 **44** 확보. 가로(39px)는 옆 세그먼트와 gap 4px 라 **일부러 안 넓혔다** |

(감사 프로브는 요소 중심에서 ±21px 를 찍어 판정한다. 40px 히트박스는 이 기준을 살짝 못
넘겨 계속 목록에 남는다 — 숫자를 예쁘게 만들려고 기준을 낮추지 않았다.)

---

## 4. 남은 위험 (NOT TESTED — 추정으로 PASS 처리하지 않음)

| 항목 | 상태 | 이유 |
|---|---|---|
| **실기기** (아이폰·갤럭시·아이패드 실물) | **NOT TESTED** | 이 세션에 실기기 없음 |
| **iOS Safari(WebKit) 엔진** | **NOT RE-TESTED** | 이번 라운드는 Chromium 에뮬레이션. 09-07/09-08 라운드가 시뮬레이터 실 Safari 로 키보드·safe-area·시스템백을 이미 검증했고, 이번 변경은 **미디어쿼리 조건 + `::after` 히트영역**이라 엔진 의존이 낮다 — 그래도 '검증했다'고 쓰지 않는다 |
| **safe-area (노치·홈 인디케이터)** | **NOT RE-TESTED** | 헤드리스는 `env(safe-area-inset-*)` 가 전부 0. 09-08 라운드 시뮬레이터 PASS 를 승계 |
| **소프트 키보드** | **NOT RE-TESTED** | 09-08 라운드에서 실제 iOS/안드 키보드로 PASS. 이번 변경과 무관 |
| **브라우저 줌 80~200%** | **간접** | 줌은 CSS 픽셀 폭 변화와 동치라 폭 60지점 스윕이 덮는다. 문자 그대로의 zoom 테스트는 안 했다 |
| **실백엔드 E2E** | **NOT TESTED** | 목 API. 로딩/에러/429 등 상태별 화면은 이번 범위 밖 |
| **다크모드** | **제외** | 프로젝트 방침(2026-05-28 보류 중) |

### 사람이 결정할 것

1. **AndroidManifest 에 `screenOrientation` 을 넣을 것인가.**
   지금은 넣지 않았다 — 넣으면 같은 APK 를 쓰는 **안드로이드 태블릿의 가로도 같이 잠긴다.**
   BUG-R1 수정으로 폰 가로는 이미 모바일 셸로 정상 동작하므로 기능상 필요는 없다.
2. **PC 셸 높이 기준 600px.** 데스크톱 브라우저 창을 세로로 아주 납작하게(높이<600) 줄이면
   모바일 셸이 된다. 의도한 동작이지만 원장이 놀랄 수 있다.

---

## 5. FINAL VERDICT

## 🟡 YELLOW — RELEASE WITH KNOWN NON-BLOCKING ISSUES

- **P0 0건.** 화면 진입 불가·조작 불가·가로 넘침으로 핵심 UI 사용 불가 — 전 구간 0건.
- **P1 2건 전부 수정·재검증 완료** (안드 폰 가로 데스크톱 UI / 320~344px 가로 넘침).
- **P2 3건 수정 완료** (인덱스바 · 터치타깃 12종 · 울트라와이드 상한).
- YELLOW 인 이유는 **남은 결함이 아니라 남은 검증**이다 — 실기기와 iOS Safari 엔진에서
  이번 변경을 직접 돌려보지 않았다. 그 두 칸이 채워지면 GREEN 이다.
