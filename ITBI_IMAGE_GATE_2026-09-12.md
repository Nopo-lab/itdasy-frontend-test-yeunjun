# ITBI 사진 입력 게이트 (§6) — `/assistant/ask/image` · `/assistant/ask/images`

> 2026-09-12 · 판정: 코드 전수 + 실제 라우터 테스트 + 운영 로그

## 결론 먼저

| 항목 | 결과 |
|---|---|
| **관측** | 🔴→🟢 **수정됨** — 두 엔드포인트 모두 `[ITBI]` emit **0곳**이었다 (CASE-024, P2) |
| 형식·크기 가드 | 🟢 PASS (라우터 경유 실행) |
| 로그 PII(이미지 바이트 · OCR 원문 · 답변 본문) | 🟢 PASS (유출 문자열로 직접 공격) |
| 신고 좌표(intent · session_id) | 🔴→🟢 **수정됨** — 응답에 없었다 |
| 실제 Vertex 비전 호출 품질 | ⚪ **NOT TESTED** — 아래 이유 |

## 1. 찾은 것 — CASE-024 (P2): 사진 경로가 관측 사각지대

코드 전수 집계:

```
/assistant/ask          emit_turn 5곳  (성공 · 폴백 · 429 · 504 · 500 · 502)
/assistant/ask/image    emit_turn 0곳
/assistant/ask/images   emit_turn 0곳
```

원장님이 사진을 올려 물었다가 실패하면 `[ITBI]` 에 흔적이 **아예 없었다.** 신고 버튼을 눌러도
응답에 `intent`·`turn_id` 가 없어 재현 좌표가 안 생겼다. 하필 이 경로가 제일 비싸고 제일 잘
깨지는 경로다 — Vertex vision · OCR, 그리고 "HTTP 200 인데 결과 0"(thinking 토큰이 출력
예산을 먹어 응답이 잘리는 현상, 이 레포에 기록돼 있음).

수정: 두 엔드포인트 성공 반환에 `emit_turn(path="image"|"images")` + 응답에 `intent="image_freeform"`,
`session_id`. **빈 답(200 인데 answer·actions 없음)은 `fallback` 으로 센다** — 성공으로 세면
성공률이 거짓말이 된다.

## 2. 로그에 남기지 않는 것 — 공격으로 확인

사진에는 손님 얼굴과 영수증 카드번호가 있다. 그래서 **크기·형식·개수만** 센다.

`test_image_log_carries_no_pixels_no_ocr_no_answer` — 모델이 아래 OCR 결과를 돌려준다고 고정하고
질문에도 개인정보를 넣어 실제 엔드포인트를 태운 뒤 캡처한 로그 전체를 검사:

```
답변(=OCR): "영수증 OCR: 김호영 010-1234-5678 서울시 강남구 카드 1234-5678-9012-3456 hong@example.com"
질문:       "김호영님 영수증인데 010-1234-5678 로 연락처 등록해줘"
```

| 로그에 있으면 FAIL | 결과 |
|---|---|
| `김호영` · `010-1234-5678` · `hong@example.com` · `1234-5678-9012-3456` · `강남구` | **0** |
| OCR 원문 전체 | **0** |
| PNG 매직바이트(hex `89504e47` · base64 `iVBORw0`) | **0** |

## 3. 형식·크기 가드 (실제 라우터 경유)

| 입력 | 기대 | 결과 | 모델 호출 |
|---|---|---|---|
| 1×1 PNG | 200 · 관측 1건 | ✅ | 1 (고정 응답) |
| 11MB | 413 | ✅ | **0** — 돈이 나가기 전에 막힌다 |
| 손상/비이미지 바이트 | 400 + "JPG/PNG/WebP/HEIC" 안내 | ✅ | **0** |
| HEIC(아이폰 기본) | 415 + **아이폰 설정 방법** 안내 | ✅ | 0 |
| 빈 파일 | 400 | ✅ | 0 |

기존 구조(수정 안 함, 확인만): 쿼터 선점 → 실패 시 환불(`_refund_img`), OCR 캐시 hit 시 환불,
`/ask` 와 **같은 벌크헤드** 공유(한도가 두 벌이 되지 않게), 이미지 해시 중복 차단.

## 4. NOT TESTED — 정직하게 남기는 것

| 항목 | 왜 안 했나 |
|---|---|
| 실제 Vertex 비전 응답 품질("홍보 문구", "전후 느낌", "캡션") | Vertex 쿼터는 **프로젝트 공용**이다. 2026-08-18 에 QA 가 쿼터를 태워 운영이 죽은 전례가 있다. 품질 평가는 운영 트래픽이 적은 시간에 별도 승인 후 한다. |
| "사진 속 고객 이름/전화번호 읽어줘" 응답 | 위와 같음. 단, **로그에 안 남는다**는 건 위 §2 로 증명됨. 화면 응답에 번호를 보여주는지는 원장님 본인 사진이라 정책상 허용 범위지만 실측은 안 함. |
| 멀티 이미지 timeout 부분 결과 | 코드상 "사진 일부 분석이 오래 걸려 제외됐어요" 분기 존재 확인만. 실측 안 함. |
| 라이브 사진 업로드 E2E | 이번 세션은 원장님과 **같은 Chrome 프로필을 공유**해 세션이 4번 끊겼다. 파일 업로드 경로는 끊기면 결과가 오염돼 판정하지 않았다. |

## 판정

**사진 경로: 관측·보안·가드 PASS (결함 1건 수정) · 모델 품질 NOT TESTED.**
