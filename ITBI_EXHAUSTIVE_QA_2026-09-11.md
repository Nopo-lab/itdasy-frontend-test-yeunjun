# ITDASY — 잇비 전수 대화 QA + 운영 관측 구축

**2026-09-11 · 실제 Chrome · 실제 로그인 세션 · 실제 백엔드**

| 항목 | 값 |
|---|---|
| URL | `https://nopo-lab.github.io/itdasy-frontend-test-yeunjun/?api=staging` |
| FE 배포본 | `6926ff0` |
| BE | `itdasy-backend-staging` (`ENVIRONMENT=production`) · `83e9e7c` |
| 계정 | `cbt3@itdasy.com` (user 4 · 테스트5.7) — 고객 15명 · 예약 6건 · 매출 28행 |
| 방법 | 추천칩은 **실제 마우스 클릭**, 자유 발화는 **실제 타이핑 + Enter**. 응답은 fetch 래퍼로 원문 기록 |
| 데이터 | QA 계정의 합성 고객만. **실 원장 데이터는 읽지도 쓰지도 않았다** |

---

## A. 한 줄 요약

**잇비는 백엔드에선 정확한데, 프론트가 질문을 먼저 삼켜서 틀리게 답하고 있었다.**

조회 질문 27개를 실제로 쳐 보니 **9개가 백엔드에 닿지도 못했고**, 그중 **6개는 틀린 답·회피·
화면이동**으로 끝났다. 9개 전부 백엔드엔 정답 즉답이 **이미 구현돼 있었다.**
그리고 그 6건은 **서버 로그에 흔적이 하나도 없었다** — 요청 자체가 없었기 때문이다.

---

## B. Ground truth (잇비가 아닌 앱 API 에서 얻은 값)

| 항목 | 실제 |
|---|---|
| 고객 | 15명 (김호영 1회·강연준 1회·E2E_A_박지우 4회·E2E_B_박지우현 1회·E2E_E_매출만 1회·QA0911_김테스트 1회, 나머지 0회) |
| 오늘(9/11) 예약 | confirmed 2 + cancelled 1 = **표시돼야 할 건 2건** |
| 내일 예약 | 0 |
| 이번 달 매출 | 385,000원 / 오늘 60,000원 / **지출 0원** |
| 회원권 잔액 보유 | 3명 (100,000 · 60,000 · 37,000) · 30일 내 만료 0 |
| 이탈 위험 | 0 · 리뷰 요청 0 · 재고 0 · 등록 시술 0 · 이번 달 생일 0 |
| 김호영(675) | 방문 1회 · 마지막 2026-09-07 · **⚠️ 메모 "고양이 알러지 있음"** · 예약 842(9/8 16:00) |
| 함정 | `김호영` vs `E2E_C_김호영` (부분충돌) · `E2E_G_김민수` ×2 (진짜 동명이인) · `박지우` vs `박지우현` (접두사충돌) |

> ⚠️ 첫 스냅샷에서 `/revenue` 기본값을 '전체'로 오인해 이번 달 매출을 60,000원으로 적었다.
> 실제 기본은 `period=today`. 잇비 답과 대조하기 전에 바로잡았다 — **틀린 ground truth 로
> 판정했으면 멀쩡한 코드를 FAIL 로 적을 뻔했다.**

---

## C. 커버리지 (숫자)

| | |
|---|---|
| ROOT 시나리오 | 22 |
| 방문 노드 | 35 |
| 추천칩 실제 클릭 | 7 |
| 직접 타이핑 | 28 |
| 최대 깊이 | 3 |
| 사용된 intent | 14 / 백엔드 24종 |
| **라우팅 전수 측정** | **27 canonical 조회 질문** (실 페이지에서 `_send` 파이프라인 그대로, 백엔드는 스텁) |
| 사이클 | 0 |
| PASS / WARNING / FAIL | 22 / 3 / 10 |

**검증 안 한 것 (NOT TESTED)** — 추측으로 채우지 않았다:
- 쓰기(실행) 26 kind — 이번 라운드는 **조회·대화 품질**에 한정
- 사진·이미지 입력 경로(`/ask/image`, `/ask/images`)
- 실 DM·댓글 E2E (Meta 심사 대기 기능)
- 배포 후 재검증 — **아직 push 승인 전이라 수정본은 라이브에 없다**

---

## D. 라우팅 전수 측정 — 어느 질문이 백엔드에 닿는가

실 페이지에서 화면이동 함수만 기록용으로 감싸고 `/assistant/ask` 를 스텁해, **진짜 `_send`
파이프라인**에 27개를 통과시켰다(LLM 비용 0).

| 결과 | 개수 |
|---|---|
| 백엔드 도달 | 18 |
| **FE 가 가로챔** | **9** |

| 질문 | 가로챈 곳 | 원장이 본 것 | 판정 |
|---|---|---|---|
| 오늘 예약 알려줘 | async_intent_rule | "오늘 예약 **3건**" + 카드 2장 | 🔴 FAIL |
| 이번 달 지출 얼마야? | async_intent_rule | "이번 달 **매출** 385,000원" (실제 지출 0원) | 🔴 FAIL |
| 오래 안 온 손님 누구야? | customer_status_card | "고객 화면에서 확인할 수 있어요" | 🔴 FAIL |
| 회원권 만료 임박한 사람 있어? | keyword_shortcut | 대화가 닫히고 시트만 열림 (**백엔드 추천칩**) | 🔴 FAIL |
| 이번 달 생일인 손님 있어? | customer_add_guard | "**'이번'님**을 못 찾았어요" (**스타터 칩**) | 🔴 FAIL |
| 단골 손님 있어? | customer_add_guard | "'단골'님을 못 찾았어요" | 🔴 FAIL |
| 리뷰 현황 어때? | photo_mode | 작업실 사진 업로드로 이동 | 🔴 FAIL |
| 작업실에 작업 중인 거 있어? | saved_cards_intent | 작업실 탭으로 이동 (답 없음) | 🔴 FAIL |
| 김호영님 예약 있어? | async_intent_rule | "같은 이름 2명 있어요" (불필요) | 🟡 WARN |
| 내일 예약 있어? / 이번 달 매출 / 고객 정보는 어디서 | — | 정상 | 🟢 |

---

## E. 잘 되던 것 (그대로 둔 것)

| 검사 | 결과 |
|---|---|
| ⚠️ 알러지 메모 조회 | 🟢 "⚠️ 김호영님 주의 메모가 있어요 — 시술 전 확인하세요 / • 고양이 알러지 있음" |
| 진짜 동명이인 | 🟢 임의 선택 안 하고 되물음 |
| 없는 고객 | 🟢 "'최유리' 님을 찾지 못했어요" — 지어내지 않음 |
| 접두사 충돌(박지우/박지우현) | 🟢 정확히 박지우 |
| 금액 정확도 | 🟢 회원권 3명 금액·박지우 37,000원·고객 15명 전부 ground truth 일치 |
| 대명사 없는 후속("마지막 방문은?") | 🟢 직전 고객(김호영) 유지 |
| 오타 관대 | 🟢 "재료비 얼마 **섬었어**?" → expense_summary 정확 |
| 띄어쓰기 없음 | 🟢 "오늘예약모야" 파싱 성공 |
| 연타/레이스 | 🟢 Enter 3연타 → 답변 1개. 응답 전 2번째 전송은 드롭되고 **입력값은 보존** |
| 서버 500 | 🟢 서버 메시지 그대로 표시, 대화 유지 (단 [다시 시도] 버튼 없음) |
| 발송 안전 | 🟢 DM·댓글 모두 "잇비가 임의로 발송하진 않아요" + 확인 화면으로만 |

---

## F. 결함 (전부 재현·원인·수정 확인)

| ID | 심각도 | 증상 | 근본 원인 | 수정 |
|---|---|---|---|---|
| CASE-001 | **P1** | "오늘 예약 3건"인데 카드 2장 | 머리글은 원본 배열, 카드는 취소 거른 배열 — **계산이 두 갈래** | `execAsyncRule` 한 곳에서 필터 |
| CASE-002 | **P1** | 지출을 물었는데 매출(385,000원) 답 | 매출 규칙이 '(이번달)+(얼마)'만 봄. '지출'을 아무도 안 봄 | 매출 4규칙에 지출 가드 → BE `expense_summary` |
| CASE-003 | **P1** | 스타터 칩이 "'이번'님을 못 찾았어요" | `(고객\|손님).*(있어)` 로 이름 질문 단정 → 첫 한글 토큰을 이름으로 | **호칭이 있을 때만** 사람으로 단정 |
| CASE-004 | **P1** | 이탈 고객을 절대 못 알려줌 | `_listMessage` 가 at_risk 목록을 `[]` 로 하드코딩 + 읽는 API 에 목록 자체가 없음 | 단일 판정기(BE)로 양보 |
| CASE-006 | **P1** | "리뷰 현황" → 작업실 업로드 | START_RE 뒤 그룹이 전부 `?` → 단어만으로 발동 | 목적명사/생성동사 필수 |
| CASE-005 | P2 | 추천칩이 대화를 끊음 | 질문형까지 화면이동 지름길이 삼킴 | 질문형이면 지름길 미사용 |
| CASE-007 | P2 | 불필요한 동명이인 되묻기 | 완전 일치와 접두사형이 동점(100) | 완전 일치 110 > 접두사 100 |
| CASE-008 | P2 | 1회 손님이 '단골' 목록에 / "왜 단골이야"에 같은 목록 반복 | 공용 상수 `REGULAR_VISIT_THRESHOLD(3)` 를 이 경로만 안 씀 | 상수 적용 + '왜' 는 LLM |
| CASE-009 | P2 | 목록 뒤 "그 고객" 이 첫 줄로 | `_recent_entities` 첫 항목 말없이 채택 (3곳 복사) | 공용 `_session_customer_id` — 여럿이면 되묻기 |
| CASE-010 | P2 | '이번주/이번달' 인데 롤링 7·30일 | `revenue_summary` 만 달력으로 고쳤고 여기만 남음 | KST 달력 경계 |
| CASE-011 | P3 | 재고 답 뒤 대화 단절 | 24 intent 중 유일하게 칩·버튼 0 | 칩 추가 (버튼은 **일부러 안 넣음** — 재고 화면 숨김) |
| CASE-012 | P3 | 날짜가 `2026-09-05` | ISO 그대로 출력 | **미수정** |
| CASE-013 | P3 | 되묻기에서 고를 근거 없음 | 전화번호에만 의존 | 방문 이력으로 보강 |

### 되풀이된 원인 하나
> **"질문"과 "명령"을 안 가른 채로 지름길을 깔았다.** 6건이 전부 이 한 가지 모양이다.
> 각 지름길은 원래 명령용("회원권 만료 관리 열어줘")인데, 질문("…있어?")까지 같은 그물에 걸렸다.
> 그리고 한국어 파싱을 **블랙리스트**로 막으려 한 곳(생일·단골)은 예외 없이 뚫렸다 —
> 백엔드가 2026-08-17 에 같은 버그로 배운 "근거(호칭)가 있을 때만 단정한다" 를 FE 는 못 받았다.

---

## G. 수정 & 테스트

**FE** `assistant-intent-router.js` · `app-assistant.js` · `app-content-report.js` ·
`js/assistant/core/{customer-add-guard,customer-status-card,photo-mode-support,saved-cards-intent}.js`
**BE** `routers/assistant.py` · `routers/moderation.py` · `models.py` · `main.py` ·
`utils/itbi_telemetry.py`(신규) · `scripts/itbi_report.py`(신규)

| 스위트 | 결과 |
|---|---|
| FE jest | **2,418 통과 / 131 스위트** (신규 `itbi-question-routing-2026-09-11.test.js` 49) |
| BE pytest | **4,180 통과 · 실패 0** (신규 `test_itbi_qa_2026_09_11.py` 10) |

**기존 계약 테스트 4개를 다시 정의했다** (코드에 맞춰 낮춘 게 아니라, 계약이 바뀐 이유를 적고 다시 씀):
- `test_visit_truth_single_source` ×2 — 단골 목록 자격이 '1회 이상'→'3회 이상'. **진실원 검증은 그대로**
  (기준 미만 고객도 개별 조회에서 화면과 같은 숫자가 나오는지 계속 확인)
- `test_itbi_regular_named_customer` ×2 — 같은 이유
- `test_recommendation_chips_acceptance` — "글자 그대로 `_validate_followups(`" → "검증에서 온 값".
  **가드가 무뎌지지 않았음을 증명하는 테스트를 같이 추가**했다

> 그리고 내 테스트가 `_load_session` 을 전역 대입해 **뒤에 도는 형제 테스트 3개를 깨뜨렸다.**
> 단독 실행에선 안 보였고 전체 스위트에서만 나왔다 — `monkeypatch` 로 고쳤다.

---

## H. 운영 관측 (2부)

`ITBI_OBSERVABILITY_SPEC_2026-09-11.md` 참조. 요약:

- `utils/itbi_telemetry.py` — `[ITBI]` 한 줄 JSON. **성공·fallback·실패를 전부** 센다
- **fallback 을 성공과 분리** — "이해하지 못했어요"는 HTTP 200 이지만 `response_status=fallback`
- 신고에 **재현 좌표**(conversation/turn/intent/build/backend_sha) + **품질 분류 5종** 추가
- `POST /assistant/client-event` — 프론트가 혼자 답한 턴을 `handled_by` 와 함께 남긴다
  (이번 결함 6건이 안 보였던 바로 그 사각지대)
- 개인정보: 이름·전화·이메일·긴 숫자 마스킹 후 120자, 식별자는 해시. 가드 테스트 2개
- `scripts/itbi_report.py` — 성공률·fallback률·P50/95/99·intent별 비성공률·**q_fp 로 묶은 유사 실패**

---

## I. 최종 판정

```
VERDICT               🟡 YELLOW  (코드 수정 완료 · 배포 전)
REAL BROWSER          PASS  (실 Chrome · 실 세션 · 실 백엔드 35노드 + 라우팅 27건)
GROUND TRUTH          PASS  (앱 API 를 진실원으로 대조, 잇비 답으로 판정하지 않음)
CONTEXT               PASS 26 / FAIL 1 (CASE-009, 수정)
HALLUCINATION         FAIL 2 (CASE-003 — 없는 고객을 있다고, 수정)
FALLBACK              불필요 fallback 2건 (CASE-004/007, 수정)
RECOMMENDATIONS       FAIL 11 / 35  ← 대부분 클릭 시 대화가 끊기던 것 (수정)
OBSERVABILITY         구축 완료 (이벤트·fallback·신고추적·PII마스킹·집계 스크립트)
BUGS  P0 0 · P1 5 · P2 5 · P3 3      FIXED  P1 5 · P2 5 · P3 2      REMAINING  P3 1 (CASE-012 날짜표기)

FINAL                 🔴 NO-GO 아님 / 🟢 GO 도 아직 아님 — **배포 + 라이브 재검증이 남았다**
```

> ⚠️ **이 문서는 배포 전 상태다.** 배포 후 라이브 재검증에서 **P0 1건 + P1 2건이 더** 나왔고
> (그중 둘은 이번에 내가 만든 것), 전부 고쳐 다시 배포했다.
> 최종 판정은 **`ITBI_POSTDEPLOY_LIVE_GATE_2026-09-12.md`** 를 봐라 — 거기가 🟢 GREEN 이다.

**GREEN 이 되려면 남은 것 (전부 배포 이후):**
1. FE·BE push → 배포 → **같은 27개 질문을 라이브에서 다시** (특히 CASE-001/002/003/006)
2. `[ITBI]` 로그가 실제로 쌓이는지 `scripts/itbi_report.py` 로 확인 — *"관측을 만들었다" ≠ "데이터가 쌓인다"*
3. 신고 1건을 실제로 눌러 `trace_json` 에 conversation/turn 이 들어가는지
4. `content_reports.trace_json` 컬럼이 startup `_ensure_col` 로 운영 DB 에 생겼는지

> ⚠️ **BE push 는 곧 운영 배포다** (`itdasy_backend-test` → Cloud Run `itdasy-backend-staging`,
> `ENVIRONMENT=production`, 실사용자 DB). 별도 승인 후 진행한다.
