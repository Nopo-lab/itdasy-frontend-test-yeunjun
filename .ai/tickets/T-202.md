# T-202 · 예약 발행 기능 제거

> **상태:** 🟢 원영님 결정 확정 (2026-04-20 17:30) · 플랜 작성 대기
> **담당:** T1 Architect (플랜·영향도 조사) → T2 FE Coder (코드 삭제)
> **트랙:** 표준 (AGENTS.md §4-A) · 파일 삭제 + 인라인 핸들러 3곳 제거 + JS 호출처 영향 조사 = 경량 트랙 아님

## 배경

원영님 17:30 결정: "b3 예약기능 없앨것임" — 프로토타입 v2 에 예약 탭이 없는 이유도 이 결정과 일치. 네비에서 숨기는 게 아니라 **기능 자체 제거**.

## IN-SCOPE (삭제 대상 파악된 것)

| 파일/위치 | 내용 | 액션 |
|-----------|------|------|
| `app-scheduled.js` (194줄) | 예약 발행 UI + API 호출 모듈 | 파일 삭제 |
| `index.html` L1079 | `<script src="app-scheduled.js">` | 태그 삭제 |
| `index.html` L216 | `<button id="schedNavBtn" onclick="openScheduledPopup()">예약 🗓️</button>` | 버튼 삭제 |
| `index.html` L163 | `<button id="openScheduledBtn">예약 발행 관리 🗓️</button>` (설정 시트 내부) | 버튼 삭제 |

## OUT-OF-SCOPE (확인 후 결정)

- 백엔드 API: `/api/scheduled-posts` 계열 엔드포인트. 본 티켓은 **프론트만** 제거. 백엔드 제거는 별도 티켓 (다른 레포, T3 담당).
- DB 스키마: `scheduled_posts` 테이블. 본 티켓 무관. 백엔드 티켓에서 처리.
- 다른 모듈이 `openScheduledPopup()` / `app-scheduled.js` 의 전역 함수를 참조하는지 전수 조사 필요 (T1 Architect 플랜 단계)

## 선결 영향도 조사 (T1 Architect)

- [ ] `grep -n "openScheduledPopup\|closeScheduledPopup\|app-scheduled" -- *.js *.html *.css` 전수 스캔
- [ ] `app-scheduled.js` 가 export 하는 전역 함수 목록 추출
- [ ] 위 전역 함수를 참조하는 다른 모듈 있는지 확인 (없으면 단순 삭제 OK)
- [ ] 서비스 워커/캐시 매니페스트에 `app-scheduled.js` 포함 여부 확인
- [ ] `SELECTOR_FREEZE.md` 의 동결 함수 목록과 교차 확인 — `openScheduledPopup` 이 동결 대상이면 제거 불가 판정 → 원영님 재결정 필요

## 승인 기준 (T2 코드 착수 가능)

T1 영향도 조사 결과:
- (a) 다른 모듈 참조 0건 AND 동결 목록 미포함 → 즉시 단순 삭제 진행
- (b) 참조 있음 → T1 이 마이그레이션 플랜 작성 후 원영님 재승인

## 수용 기준 (T2 완료 시점)

- [ ] `app-scheduled.js` 파일 없음
- [ ] `grep -r "scheduled\|openScheduled" -- *.html *.js` 결과에서 제거 대상 0건
- [ ] 홈·작업실·AI추천·마무리·내샵 5탭 정상 동작 (스모크 테스트)
- [ ] ESLint / Stylelint 통과
- [ ] 라이트/다크 스크린샷 각 1장 (설정 시트 열어서 "예약 발행 관리" 없어진 것 인증)

## 순서 결정

**T-200 재스코프와 무관하게 독립 진행 가능.** 단, **P2.5 (하단 네비 이식) 이전에 하는 것이 유리** — 네비 구조 재작성할 때 삭제된 버튼을 신경 쓸 필요가 없어짐.

**권장 순서:** T-202 → P2.5 → P2.6 → P2.7 → P2-re → P3-re → P4 → P5 → P6
