# T-007 · 모듈 설명서 3개 오타·틀린 정보 정정

## 🗣️ 원영님용 한 줄

**갤러리·페르소나·공통기반** 설명서에 틀린 내용 있어요. 코드는 안 건드리고 **설명서(지침서)만** 고침.

- 갤러리: 앱 실행 순서표가 짧게 줄여쓴 거라 틀림 → 실제 21개 순서로 교체
- 페르소나: "함수 50줄" 이냐 "30줄" 이냐 숫자가 설명서 두 개에서 달라서 헷갈림 → 50줄로 통일
- 공통기반: 로그인 토큰 저장 방식 공식이 실제 코드랑 달라서 오해 소지 → 실제 코드 복사

---

**상태:** READY TO START
**Phase:** 1 (출혈 막기)
**신호등:** 🟡 노랑 (문서 수정만, 코드 변경 0)
**의존:** T-001 완료 (✓)
**예상 소요:** 20분

## 배경

T-001 (모듈별 CLAUDE.md 5개 배치) 리뷰 과정에서 3건 부정확성 발견. 코드 변경은 없고 문서만 수정.

## 수정 항목

### P2 · `js/gallery/CLAUDE.md` 스크립트 로드 순서 갱신

**현재 (부정확):**
```
app-core.js → app-portfolio.js → app-gallery.js (monolith)
  → app-gallery-bg.js → -element.js → -finish.js → -review.js → -write.js
```

**수정 (index.html:1084-1106 기반 실제 순서):**
```
app-core → app-spec-validator → app-instagram → app-caption → app-portfolio → app-ai
→ app-gallery (monolith)
→ app-gallery-bg → -element → -review → -write → -finish
→ app-persona → app-scheduled → app-story-template → app-sample-captions
→ app-push → app-oauth-return → app-haptic → app-theme → app-plan → app-support
```
마지막 문장 추가: "서브모듈이 monolith 의 전역 함수를 참조하므로 순서 깨지면 즉시 크래시."

### P3 · `js/persona/CLAUDE.md` 30줄 vs 50줄 모순 해소

**현재:**
> 기존 함수 수정은 허용하되, **30줄 이상 새로 추가**되는 경우 분할 PR 필수.

**수정:**
> 기존 함수 수정은 허용. 단 **함수 하나당 50줄 상한** (AGENTS.md §3 준수). 50줄 초과 시 새 파일 `js/persona/*.js` 로 추출 PR 필수.

### P4 · `js/core/CLAUDE.md` 토큰 키 계산 방식 정정

**현재 (부정확):**
> localStorage 키는 여기서 상수로 관리 (`_TOKEN_KEY = 'itdasy_token::' + ENV`)

**수정 (app-core.js:33 실제 구현 기반):**
> localStorage 키는 `app-core.js` 상단에서 상수로 관리:
> ```js
> const _TOKEN_KEY = 'itdasy_token::' +
>   (API.includes('localhost') ? 'local' :
>    API.includes('staging') ? 'staging' : 'prod');
> ```
> `API` 상수값으로 환경 자동 판별. 새 환경 추가 시 이 삼항 연산만 건드림.

## 수용 기준

1. 3개 파일의 해당 섹션이 위 수정안과 일치
2. 다른 섹션 변경 없음 (diff 최소)
3. 문서 상호 모순 없음 (AGENTS.md §3 vs persona/CLAUDE.md)

## 자가검토

10개 체크리스트 중 해당되는 것만 (문서 변경이라 코드 체크는 "N/A").

## 담당

T1 Architect (문서 작업이 본업). 자가 집행 후 SESSION_STATE 업데이트.
