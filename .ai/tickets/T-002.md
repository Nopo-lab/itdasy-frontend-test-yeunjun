# T-002 · ESLint + Stylelint + husky + lint-staged 설치

**상태:** pending
**담당:** T2 FE Coder (미배정)
**의존:** T-001 (AGENTS 배치 후)
**브랜치 예정:** `fe/T-002-harness-setup`
**위험 라벨:** `risk:integration` (설정 변경)

## 목표

자동 강제 시스템 1차 설치. **규칙 2개만 `error`**, 나머지 `warn`. 점진 승격.

## Acceptance Criteria

- [ ] `package.json` devDependencies 에 eslint, stylelint, stylelint-config-standard, husky, lint-staged, jest 추가
- [ ] `.eslintrc.js` 생성:
  - `no-empty: [error, { allowEmptyCatch: false }]`
  - `no-restricted-syntax: [error, { selector: "Literal[value='itdasy_token']", message: "레거시 토큰 키 금지. window.getToken() 사용." }]`
  - `no-console: [warn, { allow: ['warn', 'error'] }]`
  - `max-lines-per-function: [warn, 50]`
  - `max-lines: [warn, 500]`
- [ ] `.stylelintrc.json` 생성 (extends: stylelint-config-standard)
- [ ] `.husky/pre-commit` 생성 (lint-staged 실행)
- [ ] `package.json` scripts:
  - `lint`, `lint:fix`, `test`, `prepare: husky`
- [ ] `lint-staged` 설정 (JS → eslint, CSS → stylelint)
- [ ] `npm install` 후 `npm run lint` 가 **에러 0건 + 경고 여러개** 로 끝남

## 영향 파일

- write: `package.json`, `.eslintrc.js`, `.stylelintrc.json`, `.husky/pre-commit`, `.lintstagedrc.json`
- 신규 의존성 추가로 `package-lock.json` 갱신

## 연동 체크리스트

- [ ] Capacitor 의존성 버전 안 건드림
- [ ] `node_modules` 는 `.gitignore` 되어 있는지 확인 (기존에 있음)
- [ ] T-003 의 토큰 키 픽스와 같이 커밋하면 안 됨 (별도 PR)
- [ ] pre-commit 훅이 로컬에서 실제 실행되는지 수동 테스트

## 주의

- `max-lines` `error` 승격은 Phase 2 완료 후. 현재 500줄 초과 파일 5개 존재.
- 첫 `npm run lint` 결과를 `.ai/test-reports/2026-04-20-lint-baseline.md` 로 저장 (베이스라인)
