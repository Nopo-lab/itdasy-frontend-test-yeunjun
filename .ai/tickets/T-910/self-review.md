# T-910 자가검토

- [x] 변경 파일: `index.html`, `__tests__/build-guard-slow-load-2026-09-16.test.js`, 이 티켓 문서.
- [x] `index.html`의 스크립트 순서를 바꾸지 않았다.
- [x] 새 `window.*` 전역을 추가하지 않았다. 검사는 즉시실행 함수 안에 있다.
- [x] 로그인 저장 방식과 API 주소를 바꾸지 않았다.
- [x] 모바일 설정을 바꾸지 않았다.
- [x] Supabase와 개인정보를 건드리지 않았다.
- [x] 50줄 넘는 새 함수를 만들지 않았다.
- [x] 빈 오류 처리를 추가하지 않았다.
- [x] 커밋 메시지에 T-910을 넣는다.
- [x] `npm run lint` 오류 0, 전체 검사 209묶음·3,224개 성공, 배포 구조 검사 성공.

## 파일 크기/분리 판단

부팅 전 복구 검사는 `index.html`에서 `app-core.js` 자체의 로드 실패를 감시해야 하므로 다른
파일로 옮길 수 없다. 기존 25줄을 같은 위치의 29줄짜리 독립 함수로 바꿨고 새 제품 파일은
추가하지 않았다.
