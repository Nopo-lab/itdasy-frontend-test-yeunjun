# 2026-09-16 실제 연동·배포 재검증

## 기준 버전

- FE 제품 코드·실제 제공 소스: `08e4bd85ae45ee9a9406474326f69aeeb38ceb3b`
- FE 실제 제공 빌드: `20260916-0254-08e4bd8`
- BE main: `9a2f314353b05246798f710fba3266a8dcae5d7b`
- BE 제공 SHA: `9a2f314353b05246798f710fba3266a8dcae5d7b`
- Cloud Run 최종 버전: `itdasy-backend-staging-00649-q9r`, 트래픽 100%, `/live` 200
- DB 변경 이력: `0068_comment_event_occurred_at`
- DB 대상: Supabase `hsxxqomfbdernepykils`, PostgreSQL `postgres`, 계정
  `postgres.hsxxqomfbdernepykils`, 서울 pooler 6543
- Storage: `user-uploads`

Cloud Run의 `ENVIRONMENT` 값은 `production`이고 서비스 이름은 staging이다. 실제 DB와 연결된
환경이므로 cbt4 시험계정의 최소 영향 정상 흐름만 실행했다. 대량·삭제·장애 주입은 실행하지 않았다.
AI 장애 시험은 사용자 번호 5와 별도 시험 표식이 모두 일치할 때만 작동하도록 잠시 켠 뒤 제거했다.

## Instagram 실제 흐름

- cbt4 로그인: 200
- 연결 상태: connected, token valid, publish/comments/messages 권한 확인
- 실제 최근 게시물 읽기: 12개
- 실제 합성 시험 게시물 발행: 성공
- 같은 처리번호 재전송: 같은 게시물 번호 반환, 중복 게시 0
- 안전한 내부 시험 댓글 공개 답장: 성공
- 같은 댓글 재전송: duplicate 처리, 중복 답장 0
- 오래된 댓글 비공개 답장: `ok=false`, `partial_success=false`,
  `error_code=dm_window_expired`, 외부업체 원문 미노출
- 댓글 실제 시각 복구 뒤 대기열: 12개에서 8개. 7월의 14일 밖 항목은 제외되고 9월 항목은
  실제 8일·12일 전으로 표시됨. 서버가 받은 시각을 댓글 시각처럼 보이던 “41분 전” 표시 제거
- 신규 댓글의 실제 비공개 답장: cbt_runzi가 합성 QA 게시물에 남긴 신규 댓글을 cbt4 대기열에서
  수집한 뒤 실제 DM 전송 성공. 같은 요청 재전송은 `duplicate=true`로 차단됐고, cbt_runzi의
  Meta 대화 화면에서 시험 문구가 정확히 1건만 존재함을 확인. 앱을 다시 연 뒤 댓글 대기 수도
  9건에서 8건으로 줄어 응대 완료 항목이 남지 않음
- 비공개 답장 증거: `live-instagram-private-reply-20260916.json` (로그인표·Meta 원문 식별자는 저장하지 않음)

## AI 실제 장애 흐름

cbt4 사용자 번호 5에만 시험 오류가 적용되도록 Cloud Run 버전 `00629-vq2`를 잠시 사용했다.
일반 이용자는 헤더와 사용자 번호가 모두 맞지 않아 영향을 받지 않는다.

- 시험: 실제 `/assistant/ask`에서 모델 시간초과 경로 실행
- 응답: 504, 행동 가능한 한국어 재시도 안내
- 성공 답변 필드: 없음
- AI 사용량: 39 → 39, 실패 차감 0
- 운영기록: `[ITBI_FAULT] ... kind=model_timeout`, 실제 고객 내용 없음
- 시험 전 AI 동의: OFF
- 시험 후 AI 동의: OFF
- 시험 설정 제거 뒤 최종 버전: `00630-gvt`, 설정 없음, 정상 응답, DB 대조 정상

## 캡션 동의·업종 일치 실제 흐름

- cbt4 모바일 화면에서 젤네일 캡션 생성 시 `consent_missing` 내부 오류문구가 노출되고 중단되는
  현상을 재현했다.
- 캡션·즉시 캡션·음성 캡션·AI 비서의 캡션 요청을 공통 AI 동의 처리에 연결했다.
- 실제 배포 뒤 같은 흐름에서 Google Cloud Vertex AI(Gemini) 처리 설명과 선택 버튼이 표시됐고,
  동의하면 원래 요청이 1회 자동 재실행되어 캡션이 생성됐다.
- 재생성 결과에서 젤네일 입력에 저장된 헤어·붙임머리 문구와 헤어 태그가 섞이는 문제를 발견했다.
- 요청 업종을 태그 판정에 우선 적용해 헤어 태그 제거를 실제 확인했다. 저장 문구도 현재 시술과
  맞지 않으면 입력과 최종 결과에서 제외하도록 BE #72에서 수정했다.
- BE #72 배포 뒤 같은 입력에서도 헤어 태그는 사라졌지만 `두상·잔머리·숱추가·생인모` 문구가
  남았다. 시술명을 직접 쓰지 않은 저장 문구도 판별하도록 BE #73에서 보강했다.
- 같은 결과가 입력에 없는 피부 톤 맞춤, 컬러·디자인 선택, 큐티클 정리를 사실처럼 써서 BE #74에서
  입력·사진 설명에 없는 구체 시술 수행 문장을 응답 전에 제거하도록 보강했다.
- BE #74 배포 뒤 헤어 문구·태그는 0이 됐지만, 업종 칸이 `기타`인 상태라 디자인 설명이 남았고
  “고객님도 만족해하셨어요”라는 입력에 없는 반응과 “오늘 잇츠데이를 고객님께”라는 어색한 첫 문장이
  생성됐다. 선택한 젤네일로 업종을 다시 알아내고 고객 반응·재현 어순을 보정하도록 BE #75를 반영했다.
- BE #75 배포 뒤 업종·시술·고객 반응은 정리됐지만 첫 문장이 `처음 잇츠데이를 고객님께` 변형으로
  다시 생성됐다. 처음 온 손님 문맥을 `처음 오신 고객님께`로 보정하도록 BE #76에 실제 문장을 고정했다.
- BE #76 이후 실제 생성에서 새로 드러난 첫 방문 문법, 입력 없는 디자인·케어·라인·컬러, 원장 감정,
  고객 상태, 과장 결과, 신체 결과, 정돈 마무리, 만족 결과 단정을 BE #77~#89에서 순차 수정했다.
- 최종 BE #89 배포 뒤 cbt4에서 같은 입력으로 10회 연속 생성했다. 10/10 성공, `consent_missing` 0,
  다른 업종 문구 0, 입력 없는 완료 결과 0, 거짓 고객·원장 반응 0, 깨진 문장 0이었다.
- cbt4 말투 데이터는 인스타 글 24개 분석, 과거 게시물 59개 지문, 분석 오류 없음이다. 생성 10건 모두
  `friendly` 말투로 기록됐고 학습 이모지 집합 밖 이모지는 0이었다.
- 증거: `live-caption-final-20260916.json`.

## 모바일 화면 접근성

368×833 실제 GitHub Pages 제공 화면에서 확인했다.

- 댓글 화면 스위치: 기능 이름 있음, 44×44, Space 키 상태 변경·원복 성공
- 댓글 화면 뒤로·설정·보내기·수정·무시·고객 보기·DM 보기: 44px 이상
- DM 설정 스위치 9개: 각 기능 이름 있음, `role=switch`, 상태 제공, 모두 44×44
- DM 설정 뒤로·저장·메뉴 추가: 모두 높이 44px
- 가격표 자동 안내 스위치: Space 키 `false → true → false` 성공, 저장 없이 원복
- 가로 넘침: 문서 너비 353, 화면 너비 353

## 개인정보 사고 대응

- 운영기록 지표 `privacy_incident_signal`: 존재·필터 확인
- 알림 `Privacy incident signal`: 사용 중, 이메일 채널 연결
- 알림 `Privacy manual incident intake`: 사용 중, 이메일 채널 연결
- 채널 `연준 (잇데이 장애 알림)`: 사용 중, Google Cloud 확인 상태 `VERIFIED`
- 실행 절차: `docs/legal/PRIVACY-INCIDENT-RUNBOOK.md`, FE #56 병합
- 합성 사고 기록 검색: 확인
- 실제 메일 수신함 열람: NOT VERIFIED
- 대체 담당자 연락망: BLOCKED

## 자동 검사

- FE: 210묶음, 3,226개 성공, 2개 제외. 제외는 성공으로 계산하지 않음
- BE: 전체 4,715개 성공, 299개 제외, 1개 예상 실패, 경고 224개. 제외·예상 실패는 성공으로 계산하지 않음
- BE 순서 섞기: 이전 기준 4,621개 성공, 299개 제외, 1개 예상 실패
- 격리 PostgreSQL 닫힌 검사: 214개 성공, 제외 0
- 매장 분리 전용 검사: 172개 성공, 24개 제외. 별도 필수 99개 성공, 제외 0
