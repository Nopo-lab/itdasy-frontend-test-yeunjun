# T-902 검토 기록

## 변경 파일

- DM: `app-dm-confirm-queue.js`, `__tests__/dm-draft-refresh-recovery-2026-09-15.test.js`
- 서버 별도 작업 가지: `backend/routers/dm_confirm_queue.py`, `backend/tests/test_dm_send_edit_duration_2026_09_15.py`
- 개발 도구: `package.json`, `package-lock.json`
- 기록: `.ai/APP_FEATURE_INDEX.md`, `.ai/BOARD.md`, `.ai/FOR_USER.md`, `.ai/SESSION_STATE.md`, `.ai/tickets/T-902.md`, `.ai/tickets/T-902/plan.md`, `.ai/tickets/T-902/self-review.md`, `output/APP_RELEASE_QUALITY_AUDIT_2026-09-15.md`, `output/release-audit-2026-09-15/dependencies-audit-after-t902.json`, `t902-final-tests.log`, `t902-final-lint.log`

## 공통 10항목

1. [x] 변경 파일 전체 목록 기록.
2. [x] `index.html`과 실행 순서 변경 없음.
3. [x] 제품 공개 전역 추가·제거 없음.
4. [x] 로그인 토큰·브라우저 저장소 변경 없음. DM 값은 실행 메모리이며 계정 변경 시 기존 정리 경로로 삭제.
5. [x] 휴대폰 설정 파일 변경·동기화 없음. 실제 양 플랫폼 빌드는 미검증으로 표시.
6. [x] DB 쿼리·권한·실제 메시지 발송 없음.
7. [x] 새 함수 50줄 이하. 기존 큰 DM 파일은 보존 함수만 확장.
8. [x] 빈 catch 추가 없음. 빈 확정 문구는 안내 후 전송 중단.
9. [x] 구현 커밋 제목에 `T-902` 포함 예정.
10. [x] 새 잠금 파일을 깨끗하게 설치해 전체 198묶음/3,171개 통과. 자동 검사 오류 0/기존 경고 203. 서버 전체 4,499개 통과, 285개 환경 사유 건너뜀, 예상 실패 1개.

## 추가 확인

- DM 새 검사 7개: 예약 확정 문구·시술 시간·겹침 경고·주소 복원, 빈 문구 차단, 실제 요청 본문, 수정 답장 시 시술 시간 전달.
- 처음에는 앞의 2개 보존 검사가 실패했고 수정 후 DM 파일 12개 검사 모두 통과.
- 개발 도구 보안 공지: 변경 전 15, 깨끗한 설치 후 0. 실행용 부품만 따로 확인해도 0.
- 새 설치에서 앱 포장 도구 8.5.2, 압축 도구 7.5.22, 고유번호 도구 11.1.1 확인.
- iOS 프로젝트를 읽고 임시 빌드 단계를 메모리 안에서 추가해 호환성 확인. 실제 파일은 쓰지 않음.
- 서버 `SendEditBody`가 30~480분을 받고 예약 실행 함수에 전달하도록 보강. 가상 DB·발송으로 성공 전달과 범위 거절 검사 3개 및 전체 4,499개 통과. 커밋 `28f561b`.
- 자동 일괄 변경 대신 `tar`와 `uuid`의 최소 강제 범위만 수정. 잠금 파일은 기존 허용 범위 안의 안전 버전도 함께 갱신.
- 실제 네이티브 동기화·빌드는 수행하지 않음. `cap doctor`의 Android 공개 파일 폴더 부재는 후속 실기기 빌드 전에 확인 필요.
- T1 독립 검토가 남은 시간 0분 카드의 잘못된 겹침 경고와 서버 입력 누락을 발견. 둘 다 수정 후 재검토 통과.
- 새 압축 도구로 앱 포장 도구의 내장 압축자료 5개를 읽고, 새 고유번호 도구로 프로젝트 ID 20개를 중복 없이 생성함.

## 파일 크기/분리 판단

기존 DM 파일은 1,042줄로 크지만 현재 화면 안의 작성값을 함께 다뤄야 해 짧은 보존·전달 함수만 수정했다. 새 제품 함수는 모두 50줄 이하다.
검사는 기존 전용 파일에 7개를 추가해 198줄이며 500줄 이하다. 개발 도구 설정은 별도 실행 코드가 아니다.
