# ITDASY — RESPONSIVE REAL-ENGINE CLOSEOUT

**DATE**: 2026-09-11
**BRANCH**: `feat/responsive-alldevice` · 커밋 `795a039` → `11ebf16`
**대상**: 워크트리 실파일을 로컬 서버로 서빙, **시뮬레이터/에뮬레이터의 진짜 브라우저**가 로드
**앞 단계 보고서**: `RESPONSIVE_ALLDEVICE_AUDIT_2026-09-11.md` (자동 스윕 960지점 + 390건)

---

## 0. 이 보고서가 쓰는 말의 정의 — 먼저 못 박는다

| 표기 | 뜻 |
|---|---|
| **PASS (REAL ENGINE)** | 시뮬레이터/에뮬레이터의 **진짜 Safari(WebKit) · 진짜 Chrome(Blink)** 에서 직접 재고 확인함. 에뮬레이션 뷰포트가 아니다. |
| **PASS (EMULATED)** | 데스크톱 Chromium 뷰포트 에뮬레이션. |
| **PHYSICAL DEVICE: NOT TESTED** | **실물 기기(진짜 아이폰·갤럭시·아이패드)는 이 환경에 없다.** 추정으로 PASS 처리하지 않는다. |
| **INHERITED** | 앞선 검증 라운드 결과를 승계. 이번에 다시 돌리지 않은 것. |

이번 라운드에서 **INHERITED 는 쓰지 않았다** — 키보드·safe-area·orientation 전부 이번에 직접 다시 돌렸다.

---

## 1. 어떻게 쟀나 — 실기기 원격 계측 하네스

실기기(시뮬) 안에서는 DevTools 를 붙일 수 없어서, 서버가 `index.html` 에 두 가지를 주입한다.

1. **목 API** — `window.fetch` 가로채기. 고객 40명(긴 한글 이름 포함)·예약 40건(하루 10건)·긴 메모.
2. **원격 JS 에이전트** — 600ms 마다 서버에 명령을 묻고, `eval` 한 결과를 다시 POST.

덕분에 **실제 WebKit/Blink 안에서** 레이아웃·히트박스·safe-area·visualViewport·콘솔에러를 직접 잰다.
화면마다 **실제로 재진입(새로고침)** 하고, 기대 오버레이가 보이는지(`opened`)를 확인한 뒤
**세로로 스크롤을 훑으며**(최대 8스텝) 감사 결과를 합친다.

> 하네스 자체가 만든 오판을 4번 잡았다 — 아래 §6 에 적었다. 그중 2건은 "없는 버그"를 만들 뻔했다.

---

## 2. 실기기 커버리지

### 2-A. 기기 · 방향

| Device | OS / Browser | Portrait 뷰포트 | Landscape 뷰포트 | 결과 |
|---|---|---|---|---|
| iPhone 17 (시뮬) | iOS 26.4 · **실제 Safari** | 402×714 | **874×292** | PASS (REAL ENGINE) |
| iPhone SE3 (시뮬) | iOS 26.4 · **실제 Safari** | 375×549 | 667×311 | PASS (REAL ENGINE) |
| iPhone 17 Pro Max (시뮬) | iOS 26.4 · **실제 Safari** | 440×796 | 956×330 | PASS (REAL ENGINE) |
| iPad Air 11" (시뮬) | iPadOS 26.4 · **실제 Safari** | 820×1048 | 1180×688 | PASS (REAL ENGINE) |
| iPhone 17 **홈 화면 앱(PWA standalone)** | iOS 26.4 · WKWebView | 402×812 | — | PASS (REAL ENGINE) |
| Galaxy (AVD `itdasy_galaxy`) | Android 10 · **실제 Chrome** | 411×651 | **960×460** | PASS (REAL ENGINE) |

> Safari 의 실제 뷰포트는 기기 포인트와 다르다 — iPhone 17 은 874pt 인데 세로에서 **714**,
> 가로에서 **292** 만 남는다(주소창·툴바). 에뮬레이션으로는 절대 안 나오는 조건이고,
> 이번 라운드의 P1 하나가 정확히 거기서 나왔다.

### 2-B. 숫자

**최종 코드(`11ebf16`) 기준 실기기 화면 감사 128건 · 레이아웃 신호 0 · JS 에러 0**
(= 가로 넘침 0 · 화면 밖 요소 0 · 같은 패널 안에서 가려진 조작요소 0 · uncaught/console.error 0)

화면 15종: 홈 · 내 샵 관리 · 예약 · 고객 · 매출 · 인스타DM · 인스타댓글 · 연결된 서비스 ·
샵 관리 · 설정 · 플랜 · 잇비 · 작업실 · 고객센터 · 회원권 (+ 사진편집기 별도)

---

## 3. 실기기에서 새로 나온 결함 — 2건, 둘 다 수정·재검증 완료

### R7 (P1) — 폰 가로에서 예약 달력이 105px 창이었다

```
Device/OS/Browser : iPhone 17 시뮬 · iOS 26.4 · 실제 Safari
Viewport          : 874×292 (가로)
Route             : 예약관리
Action            : 내 샵 관리 → 예약관리 진입 후 달력 영역 실측
Observed          : 헤더 66 + 간격 18 + 툴바(월·주+범례) 58 + 통계바 45 = 머리만 187px(64%).
                    달력엔 105px, 요일줄 25 를 빼면 80px — 칸 하나(151px)도 안 들어가
                    **화면에 온전히 보이는 날짜칸 0개**
Expected          : 가로에서도 한 주는 보이고 날짜를 누를 수 있어야 한다
Root Cause        : 달력이 세로 예산을 장식(범례·통계바)과 넉넉한 패딩에 먼저 쓴다.
                    795a039 가 셸을 모바일로 되돌린 뒤에야 드러난 문제
Fix               : `@media (height <= 430px)` 에서만 범례·통계바를 접고 헤더/툴바 패딩 축소,
                    칸 max-height 84px. 세로(>430px) 값은 하나도 안 바뀐다
Same-condition    : 같은 기기·같은 방향 재측정 — 머리 187→124 · 달력 105→168 ·
                    **온전히 보이는 칸 0→7개** · 칸 중심 hit-test 성공
Adjacent viewport : 956×330(Pro Max 가로) PASS · 667×311(SE3 가로) PASS · 960×460(안드 가로) PASS
Regression        : 402×714 세로 재측정 값 불변 · 세로→가로→세로 전환 정상
Status            : FIXED (`11ebf16`)
```

### R6 (P2) — 결제 팝업 약관 링크가 14px

```
Device/OS/Browser : iPhone 17 시뮬 · iOS 26.4 · 실제 Safari
Viewport          : 402×714
Route             : 이용 플랜(#planPopup) 하단
Observed          : '이용약관' 37×14 · '개인정보처리방침' 74×14 (11px 본문 안 inline 링크)
Expected          : 결제 화면의 법적 고지 링크는 44px 확보
Root Cause        : 자동 스윕(크로미움)에선 스크롤 밖이라 안 잡혔다 — 실기기 스크롤 스윕에서 드러남
Fix               : `.legal-link` + `::after` 세로 44. 가로는 안 넓힘(둘이 ' · ' 8px 사이로 붙어 있다)
                    ⚠️ 로그인 화면의 같은 링크는 **일부러 제외** — `<label>` 안이라 넓히면
                       '필수 약관 동의' 체크박스 탭을 링크가 가로챈다
Same-condition    : 같은 기기 실측 38×44 / 75×44
Status            : FIXED (`11ebf16`)
```

---

## 4. 요구된 검증 항목별 결과

### 4-1. iOS Safari 집중 (§6)

| 항목 | 결과 | 근거 |
|---|---|---|
| 실제 Safari 뷰포트(주소창 포함/축소) | **PASS (REAL ENGINE)** | 402×714(툴바 있음)·402×812(standalone) 모두 감사 |
| 스크롤 후 viewport 변화 | PASS | 스크롤 스윕 중 `innerHeight` 714↔712 변동에도 신호 0 |
| orientation 변경 | PASS | 세로→가로→세로, 3기기에서 수행 |
| fixed 헤더/하단 내비/플로팅 버튼 | PASS | 매 스텝 hit-test — 가려짐 0 |
| modal · bottom sheet · editor toolbar | PASS | 시트 9종 + 편집기 열고 감사 |
| safe-area · home indicator | **PASS (REAL ENGINE)** | §4-3 |

### 4-2. 소프트 키보드 — **양쪽 엔진에서 직접 재현**

시뮬레이터의 하드웨어 키보드를 끄고(I/O ▸ Keyboard ▸ Connect Hardware Keyboard 해제)
**실제 한글 소프트 키보드**로 수행.

| | iOS 26.4 실제 Safari | Android 10 실제 Chrome |
|---|---|---|
| 입력창 포커스 | `customerSearch` ✅ | `customerSearch` ✅ |
| 키보드 높이(실측) | `innerH 714 − vv.h 377` = **337px** | `innerH 786 − vv.h 474` = **312px** |
| 입력창이 키보드에 가리는가 | **안 가림** (visual viewport 안) | **안 가림** |
| `kb-open` 클래스 | 적용 ✅ | 적용 ✅ |
| 하단 탭바·FAB | `visibility:hidden` (입력 방해 없음) | `visibility:hidden` |
| `--tab-bar-bottom` | `calc(14px + 0px + 337px)` | `calc(14px + 24px + 311px)` |
| 실제 타이핑 | 한글 `ㅛㄱ` 입력됨 ✅ | `kim` 입력됨 ✅ |
| 닫은 뒤 복구 | `vv.h` 714 복귀 · 탭바 복구 ✅ | `vv.h` 787 복귀 · 탭바 복구 ✅ |

🔑 **이번 변경의 최대 위험을 여기서 닫았다** — `@media (height >= 600px)` 로 PC 셸을 가르는데,
키보드가 그 값을 흔들면 **타이핑 중에 셸이 바뀐다.** 실측 결과 두 엔진 모두
키보드는 **visual viewport 만** 줄이고 `matchMedia('(height >= 600px)')` 는 **true 로 유지**된다.
(뷰포트 메타에 `interactive-widget` 지정이 없어 Chrome 기본값 `resizes-visual` 이 적용된다.)

### 4-3. Safe Area — **홈 화면에 추가(standalone)로 실검증**

Safari 브라우저 모드에선 `env(safe-area-inset-*)` 가 0 이라 의미가 없다.
그래서 **공유 ▸ 홈 화면에 추가 ▸ 웹 앱으로 열기** 로 설치해 standalone 으로 띄웠다.

| | 값 |
|---|---|
| `navigator.standalone` | **true** |
| 뷰포트 | 402×812 |
| `env(safe-area-inset-bottom)` | **34px** (홈 인디케이터) |
| 하단 탭바·잇비 FAB 아래 여백 | **48px** = 34(인디케이터) + 14(디자인 여백) → **겹침 없음** |
| `--tab-bar-bottom` | `calc(14px + 34px + 0px)` — safe-area 항이 **실제로 먹고 있다** |
| standalone 화면 감사 | 신호 0 |
| 가로에서 | `env(safe-area-inset-bottom)` = 20px 로 잡히고 겹침 0 |

### 4-4. R1 의 600px 경계 — 최종 판정

**요청한 경계값 실측** (Chromium, `PASS (EMULATED)` — 미디어쿼리 판정은 엔진 무관):

| 뷰포트 | 셸 | sideNav | body padding-left | 탭바 |
|---|---|---|---|---|
| 768×599 / 800×599 / 1024×599 / 1280×599 / 1366×599 | **MOBILE** | none | 0px | 보임 |
| 768×600 / 800×600 / 1024×600 / 1280×600 / 1366×600 | **PC** | flex | 232~260px | 숨김 |
| 767×900 | MOBILE | none | 0 | 보임 |
| 768×900 | PC | flex | 232px | 숨김 |

**실기기 교차검증 (REAL ENGINE)**

| 기기·방향 | 뷰포트 | 셸 | 판정 |
|---|---|---|---|
| iPad Air 가로 | 1180×688 | **PC 유지** | 의도대로 |
| iPad Air 세로 | 820×1048 | PC 유지 | 의도대로 |
| Galaxy 가로 | 960×460 | **MOBILE** | 의도대로 |
| iPhone 17 가로 | 874×292 | MOBILE | 의도대로 |
| Pro Max 가로 | 956×330 | MOBILE | 의도대로 |

**수정 전/후 A·B (같은 실기기·같은 방향·같은 뷰포트 960×460, Android Chrome)**

| | sideNav | body padding-left | 하단 탭바 |
|---|---|---|---|
| 수정 전 (`main`, 포트 8098) | **flex 232px** | **232px** | **숨김** |
| 수정 후 (`feat/...`, 포트 8099) | none | 0px | 보임 |

→ **600px 은 목적을 정확히 만족한다.** 태블릿·노트북 가로는 PC 를 유지하고 폰 가로만 모바일로 온다.
키보드로도 안 흔들린다(§4-2). 데스크톱 창을 세로 600px 미만으로 줄이면 모바일 셸이 되는데,
이건 의도한 동작이다(599px 높이에 데스크톱 셸을 넣으면 콘텐츠가 남지 않는다).

### 4-5. Touch Target — 실기기 실측

실기기에서 **요소 중심에서 상하좌우로 밀며 hit-test** 해 유효 히트박스를 쟀다.

- 앞 커밋(795a039)에서 고친 12종 + 이번 R6 2종 = **전부 44×44 실측 확인**
- **사진편집기**(iPhone standalone): 화면에 보이는 조작 14개 전부 유효 **44×44**, 가려짐 0
- 남은 미달 4종은 전부 사유 확인:

| 항목 | 실기기에서 확인한 사유 |
|---|---|
| `.dmm-tg` 켜기 (15+14건) | 상단 마스터 토글 OFF 상태라 `pointer-events:none`(실기기 computed 로 8/9 확인). 활성 토글은 **44×40** — 아래 여유가 7px 뿐이라 이게 물리적 상한 |
| `revenue .ar ›` (4건) | 다음 달로 못 가는 **비활성**(`pointer-events:none`) |
| `.dmm-addbtn` (1건) | 위와 같은 dim 상태 |
| 예약 월/주·오늘 | 세그먼트라 가로 확장 금지(기존 14-b 판단 유지). 세로는 이미 44 |

> **44×44 를 채웠어도 인접 오작동이 나면 FAIL** 이라는 기준에 맞춰, 매 감사마다
> "모든 버튼의 중심 hit-test 가 자기 자신을 돌려준다" 를 같이 쟀다 — **128건 전부 0건**.

### 4-6. 긴 콘텐츠 (실기기)

목 데이터에 긴 한글 고객명(`박지혜사장님소개로오신아주긴이름의고객님`)·긴 샵 이름·긴 시술명·
긴 메모·고객 40명·예약 40건(하루 10건)을 넣은 상태로 위 128건을 전부 감사했다.
→ **clipping 0 · 가로 스크롤 0 · 버튼 넘침 0 · modal 넘침 0 · fixed 겹침 0**

### 4-7. 사진편집기 (실기기)

| | iPhone 17 standalone (402×812) |
|---|---|
| 열림 | ✅ `ItdEditor.isOpen() === true` |
| 스테이지 | 402×503 (4:5 비율 유지) |
| 페이지 가로 넘침 | 0 |
| 보이는 조작 14개 유효 히트박스 | **전부 44×44** |
| 가려진 조작 | 0 |
| 실제 터치 | 우측 레일 **T(글자 추가)** 탭 → 텍스트 레이어 `내용을 입력하세요` 생성 ✅ |

가로(874×292)·태블릿·데스크톱(3440 포함) 배치는 앞 라운드 14뷰포트 결과 유지.

### 4-8. 실제 업무 동선 (실기기 · 실제 터치)

iPhone 17 에서 **손가락 탭 좌표로** 수행(에이전트 호출 아님):

| # | 실제 조작 | 결과 |
|---|---|---|
| 1 | 하단 탭바 **'내 샵 관리'** 탭 | `tab-dashboard` 활성 ✅ |
| 2 | **'예약관리'** 행 탭 | `#cal-overlay` 열림 · 보이는 날짜칸 35 ✅ |
| 3 | 날짜칸 탭 | 반응(에러 0) — 상세 시트는 안 열림(칩 영역 탭으로 판단, 결함 아님) |
| 4 | 고객관리 → **고객 행 탭** | 고객 상세 열림(`방문/매출/시술사진/메모` 렌더) ✅ |
| 5 | 검색창 탭 → 한글 입력 → 닫기 | §4-2 |
| 6 | 사진편집기 **T** 탭 | 텍스트 레이어 생성 ✅ |
| 7 | 세로↔가로 전환 | 셸·달력·시트 전부 정상 ✅ |

전 과정 **console error / uncaught 0건**.

### 4-9. 회귀

| | |
|---|---|
| jest | **1611 / 1611 PASS** (81 suites) |
| stylelint / eslint | 0 |
| pre-commit smoke (`--git`) | PASS (91 scripts · 182 lazy) |
| 자동 폭 스윕 960지점 | 가로 넘침 0 · 텍스트 잘림 0 (앞 보고서) |
| 기기 매트릭스 390건(에뮬레이션) | 레이아웃 신호 0 (앞 보고서) |

---

## 5. 남은 위험 — 정확히 적는다

| 항목 | 상태 | 설명 |
|---|---|---|
| **실물 기기** (진짜 아이폰·갤럭시·아이패드) | 🔴 **PHYSICAL DEVICE: NOT TESTED** | 이 환경에 실물이 없다. 시뮬레이터/에뮬레이터는 **엔진·키보드·회전·safe-area 는 진짜**지만, 실제 터치 정확도·기기별 폰트 렌더·저사양 성능·제조사 커스텀 브라우저는 대체하지 못한다 |
| macOS Safari 데스크톱 창 크기 테스트 | 🟡 미수행 | AppleScript 자동화가 권한 대기로 멈춰 포기. 600px 경계는 Chromium 실측 + 실기기 5종 교차검증으로 대체 |
| 실백엔드 E2E | 🟡 미수행 | 목 API. 로딩/429/네트워크 오류 상태별 화면은 이번 범위 밖 |
| 다크모드 | ⚪ 제외 | 프로젝트 방침(2026-05-28 보류) |
| Galaxy Fold 접힘/펼침 실기 | 🟡 미수행 | 뷰포트(344·585)는 자동 스윕에서 커버, 실제 폴딩 전환은 미검증 |

### 실물 기기로 10분 안에 닫는 체크리스트

1. 갤럭시(안드로이드 앱 또는 크롬)에서 **가로로 눕히고** 예약관리 → 한 주가 보이는지, 하단 탭바가 있는지
2. 아이폰에서 홈 화면에 추가 → 실행 → 하단 탭바가 **홈 인디케이터와 안 겹치는지**
3. 고객 검색창 탭 → 한글 입력 → 입력창이 키보드에 안 가리는지 → 닫고 탭바 복구
4. 아이패드 **가로**에서 사이드바가 유지되는지
5. 결제(플랜) 화면 하단 **이용약관 링크**를 손가락으로 눌러지는지
6. 사진편집기 열고 우측 레일 아이콘들이 한 번에 눌리는지

---

## 6. 하네스가 만든 오판 4건 — 기록

실기기 계측은 도구가 먼저 거짓말을 한다. 이번에 잡은 것:

1. **`scroll-behavior:smooth`** — `scrollTop` 대입 **직후** 값은 옛값이다. 그대로 비교하면
   "스크롤 안 됨" 으로 오판해 **1스텝만 감사**하고 넘어간다. 기다렸다가 비교하도록 고쳤다.
2. **`simctl openurl` 은 탭을 새로 연다** — 옛 탭의 에이전트가 명령을 가로채 **수정 전 페이지 결과**를
   돌려줬다. 에이전트에 버전 게이트 + 숨은 탭 침묵을 넣어 차단.
3. **오버레이가 겹친 채 감사** — 화면 전환을 새로고침 없이 하면 시트 10개가 쌓인다.
   이 상태에서 사진편집기 조작 14개가 전부 "안 눌림" 으로 나왔다 —
   범인은 **내가 앞 화면에서 띄워둔 `#planPopup`(z 11500)** 이었다. 새로고침 후 0건.
4. **부팅 판정을 `#tab-home` 으로 못박음** — 앱이 마지막 탭을 기억해 홈이 숨겨져 있으면
   영영 통과 못 해 화면마다 18초를 버렸다.

> 1·3 은 **없는 버그를 만들 뻔했고**, 2 는 **고친 걸 안 고쳤다고 할 뻔했다.**

---

## 7. FINAL VERDICT

## 🟢 GREEN — RESPONSIVE RELEASE READY (실엔진 기준)

**단, 한 줄은 반드시 같이 읽어야 한다: `PHYSICAL DEVICE: NOT TESTED`.**

| GREEN 조건 | 결과 |
|---|---|
| P0 = 0 | ✅ |
| P1 = 0 | ✅ (R1·R2·R7 전부 수정·재검증) |
| iPhone 검증 | ✅ **REAL ENGINE** (SE3·17·17 Pro Max, 실제 Safari) / 🔴 실물 아님 |
| Galaxy/Android 검증 | ✅ **REAL ENGINE** (실제 Chrome, 세로·가로) / 🔴 실물 아님 |
| iPad 검증 | ✅ **REAL ENGINE** (iPad Air 11, 세로·가로) / 🔴 실물 아님 |
| iOS Safari(WebKit) | ✅ |
| Android Chrome(Blink) | ✅ |
| Portrait / Landscape | ✅ / ✅ |
| safe-area 실검증 | ✅ standalone 에서 34px 실측·겹침 0 |
| keyboard 실검증 | ✅ 양쪽 엔진, 한글 입력까지 |
| orientation transition | ✅ 3기기 세로↔가로↔세로 |
| photo editor | ✅ 실기기 44×44 · 실제 터치 동작 |
| 핵심 업무 시나리오 | ✅ 실제 터치 7단계 |
| regression | ✅ jest 1611/1611 · lint 0 · smoke PASS |
| 자동 폭 스윕 960지점 | ✅ overflow 0 · clipping 0 · layout signal 0 |

**연준님 정의의 GREEN 이 '실물 기기'를 요구한다면 이 보고서는 🟡 YELLOW 로 남는다.**
그 경우 §5 의 6줄 체크리스트만 실물에서 통과하면 GREEN 이다.
코드 쪽에서 막고 있는 것은 없다.

### 최종 질문에 대한 답

> 원장이 아무 iPhone·Galaxy·Android·iPad·노트북·데스크톱을 잡고 세로든 가로든 써도
> ITDASY 의 핵심 업무를 실제로 수행할 수 있는가?

**실제 Safari·실제 Chrome 위에서 128건을 직접 재고 실제 손가락으로 눌러본 범위에서는 YES.**
가장 가혹한 조건이던 **아이폰 가로(874×292, 화면의 64%가 머리였던 예약 달력)** 도 이번에 닫았다.
남은 것은 실물 기기 확인 하나뿐이다.
