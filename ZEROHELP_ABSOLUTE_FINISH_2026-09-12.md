# ITDASY — ZERO-HELP FIRST OWNER · ABSOLUTE FINISH 라운드 결과

- 라이브 빌드: `20260912-0131-df33143` (하드 새로고침 후 실행 번들 문자열로 확인)
- FE origin/main: `df33143`
- 로그인: `/auth/me` 200 · id=4 · `테스트5.7`
- SW 캐시: `itdasy-20260912-0131-df33143` 단일 (stale 없음)
- 뷰포트: 606×717 · dpr 1 (Chrome 확장은 이 아래로 못 내려감 — §10 참조)
- 테스트 시작 슬롯: `mtu4ptpma7gvc`, `mtwaanxzx4nb6`, `mtwaanxzx4nb6_conflict_1789107327563` (전부 내 것 아님)

---

## 최종 판정

# 🔴 RED

이유는 하나다. **P0/P1/P2 는 0 이 됐지만 UNTESTED 가 0 이 아니다.**
GREEN 조건이 `UNTESTED = 0` 을 요구하므로 GREEN 을 쓸 수 없다.
아래에 닫은 것과 안 닫은 것을 하나씩 적는다.

---

## 1. 이번 라운드에 닫은 것 (실앱 증거)

### P2-1 · 캐러셀 3장을 재편집하면 사진이 1장만 들어오던 것 — 수정·배포·실검증 완료

**원인 (두 겹)**

| # | 무엇 | 결과 |
|---|---|---|
| ① | 장별 저장(`onDone` 의 perPhoto 합성)이 만든 `editState.photos` 가 **자기 원판 1장** | `_restoreState` 가 `S.photos` 를 그 1장으로 덮어씀 → 편집기 썸네일 1개, 2·3번 장을 **다시 고칠 방법이 없음** |
| ② | 전체 스냅샷(저장 순간 보던 장)을 집어도 `layersByPhoto` 는 저장에 안 실림 | 다른 장이 **빈 채로** 열리고, 썸네일로 장을 넘기는 순간 `_switchPhotoLayers` 가 그 빈 상태를 저장 → **글자 소실**. 1장짜리 스냅샷은 `fitMode:'contain'`·`adj:[]` 고정이라 채우기·보정도 되돌아감 |

**수정** (`js/workspace/workspace-v2-flow.js` + `js/itd-editor/itd-editor.js`)
재편집이면 전 장을 통째로 복원한다 — **원판(cleanBase) 목록** + **장별 레이어** + **활성 장 번호**.
전역 값(비율·채우기·보정·구도)은 '전 장을 담은' 스냅샷에서 가져온다.
원판을 쓰는 이유: 편집본(`editedDataUrl`)을 넘기면 레이어가 **두 번 구워진다**.
콜라주(여러 장을 한 장으로 합치는 모드)는 사진 목록의 의미가 달라 건드리지 않는다.

**라이브 검증 (3회 열기/저장 왕복)**

| 항목 | 결과 |
|---|---|
| 저장본 `photos.length` | **3** |
| 편집기 '보정할 사진을 고르세요' 썸네일 | **3** (수정 전 1) |
| A/B/C 각각 text 살아 있음 | `AAA-HAIR` / `BBB-NAIL` / `CCC-LASH` — 각자 자기 장에 ✅ |
| A/B/C 각각 보정 살아 있음 | A 밝기 **+16**(b116), B 채도 **+53**(s153), C 중립 ✅ |
| 이중 굽기 없음 | 스테이지 배경 해시 vs 발행본 해시 대조 — B `b75420e1`≠`3be275d0`, C `edfbd0f0`≠`509d5c3b` ✅ |
| 저장→재편집→저장→재편집 안정성 | 3회차에도 썸네일 3 · `AAA-HAIR` · 밝기 +16 ✅ |
| console error | 앱 오류 **0** (잡힌 7건은 전부 누끼 WASM 의 `INFO: Created TensorFlow Lite XNNPACK delegate` — Sentry 의 console 계측이 ERROR 로 올린 것) |

가드 10개 신설 · 돌연변이 **14종 전부 잡힘** (`js/workspace/__tests__/carousel-reedit-all-photos-2026-09-12.test.js`)

### P2-2 · `_syncOutputForEdit` — 새 빌드에서 실검증 완료

하드 새로고침으로 실행 번들이 `df33143` 인 것을 먼저 확인한 뒤 측정.

| 항목 | 결과 |
|---|---|
| `storyEdited` | `[true, true, true]` ✅ |
| `templateOutputs` | 3개 · 각 `photoIds` 1개 · 사진과 1:1 ✅ |
| 사진 뒤바뀜 | 없음 — out0=헤어+AAA / out1=네일+BBB / out2=야외+CCC (눈으로 확인) |
| 글자 소실 | 없음 (1차·2차·3차 저장 모두) |
| 발행 해상도 | 3장 모두 **1080×1350** (4:5) |

### §4 · 캐러셀 파괴 불변식 — PASS

`A→B→C→A→C→B` 6회 전환 후 B 에 글자 추가 → 저장.
매 전환 화면에서 사진·글자·보정이 전부 자기 것. 저장 결과:

```
A: ["AAA-HAIR"]
B: ["BBB-NAIL","B2-EXTRA"]   adj b116 / s153 / 중립
C: ["CCC-LASH"]
```
섞임 0 · 소실 0. **재편집된 캐러셀에서 장별 격리가 확인된 건 이번이 처음이다** (이전엔 새로 만든 캐러셀에서만 6/6 이었다).

### §19 · 테스트 데이터 정리 — 완료

내가 만든 슬롯 `mtxptd382ysjb` 하나만 삭제. 로컬 IDB·서버 양쪽에서 사라진 것 확인.
남은 슬롯 = 시작 시점과 동일한 3개. `mtu4ptpma7gvc` 와 그 안의 `LIVE1080` **건드리지 않음**.

---

## 2. 이번 라운드에 **안 닫은** 것 — UNTESTED

아래는 이 라운드에서 **실행하지 않았다**. 코드가 어떻든 증거가 없으므로 결과를 쓰지 않는다.

| 절 | 항목 | 상태 |
|---|---|---|
| §5 | S1–S20 전 시나리오 | UNTESTED (이번 라운드 실행한 건 S1 성격의 1회 완주뿐) |
| §6 | S20 신규 원장 ×5 | UNTESTED |
| §7 | 잇비 자연어 체인 10문장 | UNTESTED |
| §8 | iPhone 코어 세트 | UNTESTED (시뮬레이터 부팅 0대 상태로 시작) |
| §9 | Android 코어 세트 | UNTESTED (에뮬레이터 미기동) |
| §10 | 출력 불변식 6뷰포트 | **부분** — 606px 에서만 확인(1080×1350). 375/390/430 은 이 표면에서 **BLOCKED**(Chrome 확장이 606px 아래로 못 내려감, `resize_window` 가 성공을 보고하면서 `innerWidth` 가 안 바뀜) |
| §11 | 저장 실패 매트릭스 14종 + sync 사망 신호 | UNTESTED |
| §12 | P3-A 패널 가림 재공격 | UNTESTED |
| §13 | P3-B 작은 레이어 | UNTESTED |
| §14 | P3-C 캡션 이미지 밀림 | UNTESTED (원인은 이전 라운드에 특정됨 — hero `<img>` 에 width/height/aspect-ratio 없음. **이번 라운드에도 재현됨**: 질문 3 답변 직후 화면이 맨 위로 올라갔다) |
| §15 | P3-D work_memory 동의 정책 | UNTESTED |
| §16 | P3-E sync 상태 신호 | UNTESTED |
| §17 | 뷰티 사진 품질 5종 | UNTESTED |
| §18 | 샵 스타일 자동 오염 체인 | UNTESTED |

---

## 3. 이번 라운드에 새로 관찰한 것 (결함 판정 아님 · 기록만)

**업로드 뒤 로컬 슬롯에서 `baseUrl === editedDataUrl` 이 된다.**
저장 후 서버 왕복을 거치면 사진 1·2 의 `baseUrl` 이 **글자가 구워진 이미지** 를 가리킨다
(해시 대조: `photo1.baseUrl` == `out1` == 글자 있는 이미지).
그런데 라이브 재편집은 **정상**이었다 — 편집기가 실제로 연 원판은 구워지지 않은 것이었고
(스테이지 해시 ≠ 발행본 해시), 업로드 1/4 화면 썸네일도 글자 없는 원판이었다.
즉 메모리상의 `d.photos` 는 멀쩡하고 직렬화된 슬롯 복사본만 두 필드가 같은 자산을 가리킨다.

지금은 무증상이지만 **`_cleanBase` 가 이 슬롯을 다시 읽는 경로**(새로고침 후 재편집 등)에서는
이중 굽기가 될 수 있다. 이번 라운드에 그 경로를 재현하지 못했으므로 **결함으로 판정하지 않는다.**
다음 라운드 1순위 확인 대상으로 남긴다.

---

## 4. 커밋

| 커밋 | 내용 |
|---|---|
| `7d47adc` | fix(편집기): 캐러셀 3장을 다시 열면 사진이 1장만 들어오던 것 — 전 장 복원 |
| `df33143` | origin/main 머지 후 배포 (GitHub Actions success) |

테스트: 148 suites / 2,601 passed / 2 skipped.

---

## 페르소나별 판정

| 페르소나 | 목표 달성? | 근거 |
|---|---|---|
| A (헤어 · 캐러셀 3장) | **YES** | 업로드→레이아웃→캡션→편집→저장→재편집 완주, 장별 글자·보정 유지 |
| B~H | **NOT PROVEN** | 이 라운드에서 실행하지 않음 |

---

# FINAL OWNER ANSWER: NOT PROVEN

막고 있던 P2 두 건은 실앱 증거로 닫혔고, 재편집된 캐러셀의 장별 격리도 처음으로 확인됐다.
하지만 S1–S20·S20×5·잇비·iPhone·Android·저장 실패 매트릭스·P3 재공격이 **실행되지 않았다.**
GREEN 은 `UNTESTED = 0` 을 요구한다. 그래서 GREEN 이 아니다.
