# ITBI 보안 · 개인정보 게이트 (§10 · §11 · §14)

> 2026-09-12~13 · 근거: 실제 Cloud Logging 전수 스캔 · 라이브 계정 · 격리 Postgres

## 1. §10 로그 PII — 실제 `gcloud logging read` 전수

### 1-1. 현재 빌드 (P0 수정 `076b766` 이후 모든 빌드)

30시간 `[ITBI]` 레코드 전수에서 **자유 텍스트 필드**(q · handled_by · error_* · fallback_reason) 스캔:

| 패턴 | 건수 |
|---|---|
| 전화번호 | **0** |
| 이메일 | **0** |
| 긴 숫자(7자리+) | **0** |
| Authorization Bearer | **0** |
| JWT(eyJ…) | **0** |
| cookie/session= | **0** |
| **이 계정 실제 고객 이름 20종** (상상한 문자열 아님) | **0** |
| 답변 본문(`a`) 필드 | **0** — `a_len` · `a_empty` 만 |

⚠️ 스캔 방법 주의: 레코드 **전체 JSON** 을 훑으면 `ts`(에폭 밀리초)가 매 줄 "긴 숫자" 로 잡혀
69/69 가 유출로 보인다. 자유 텍스트 필드만 봐야 한다 — 처음에 그렇게 틀릴 뻔했다.

### 1-2. 🟠 과거 로그에 **남아 있는** 유출 (P1 · 미해결 · 결정 필요)

같은 스캔에서 **21건**이 걸렸다. 전부:
- `backend_sha = 38510bbf` (P0 수정 **이전** 빌드)
- `2026-09-11 22:13 ~ 22:44 UTC` (31분 창)
- 필드 `a`(답변 본문) — 이름이 든 것 5건: `E2E_A_박지우` · `E2E_G_김민수` · `QA0911_김테스트` · `QA0912_중복테스트` · `E2E_F_회원권만`

즉 **지금은 새지 않지만, 새었던 기록이 Cloud Logging 에 그대로 있다.**
이전 게이트 보고서의 "PII leak 0" 은 수정 이후 창 기준으로는 맞았지만 이 잔존분을 다루지 않았다.

실제 피해 범위: QA 계정(user 4)의 **E2E 테스트용 가상 이름**뿐이다. 실제 원장님 손님 이름은 없다.

**제가 지우지 않은 이유**: Cloud Logging 은 **개별 항목 삭제를 지원하지 않는다.**
선택지는 둘뿐이고 둘 다 제가 혼자 정할 일이 아니다.

| 선택지 | 효과 | 위험 |
|---|---|---|
| A. 보존기간 만료 대기(기본 30일, ~2026-10-11) | 자동 소멸 | 그때까지 로그 열람 권한자에게 보임 |
| B. `gcloud logging logs delete` 로 해당 로그 통째 삭제 | 즉시 소멸 | **그 서비스의 모든 앱 로그가 같이 사라진다**(되돌릴 수 없음 · 장애 추적 불가) |

권장: **A** — 가상 이름뿐이고, B 는 운영 추적 능력을 잃는 비용이 더 크다.

## 2. §11 기억(facts) 오염 — **0**

라이브(user 4) — 질문형 8문장을 보내기 전후 `/assistant/facts` 비교:

| 원장님 말 | 잇비 응답 | 저장 |
|---|---|---|
| "김호영님 메모해둔 거 있어?" | ⚠️ 알러지 메모 표시 (`customer_memo`) | **0** |
| "박지우님 전화번호 알려줘" | 기록 없음 안내 | **0** |
| "E2E_A_박지우님 이메일 있어?" | 기록 없음 안내 | **0** |
| "김호영님 예약 메모 확인해줘" | 예정 예약 없음 | **0** |
| "김민수님 알러지 메모 있어?" | 동명이인 2명 되묻기 | **0** |
| "내가 전에 말한 고객 메모 뭐야?" | (새 대화에서) 누구인지 되물음 | **0** |
| "단골 누구야?" · "오늘 예약 몇 개야?" | 조회 답 | **0** |

**facts 2건 → 2건. 추가 0.** 기존 2건(`답변 짧게` · `고양이 알러지 주의`)은 명시 저장이다.
저장형 문장은 이전 게이트(CASE-015)에서 "보정 전에 항상 확인하는 거 기억해줘" 가 저장되고
질문형이 막히는 것을 유닛·라이브로 확인했다 — 이번에 저장형을 **다시 쓰지는 않았다**(운영 DB 쓰기 최소화).

### 오판할 뻔한 것
처음 돌렸을 때 새 대화에서 "그 고객 메모 뭐야?" 가 **특정 고객의 알러지 메모**를 보여줘서
"선행어 없는 대명사를 아무 고객으로 단정한다" 로 적을 뻔했다. 원인은 **내 하네스**였다 —
`/session/reset` 으로 새 세션을 만들고 **앱에 알려주지 않아서** 옛 세션 id 가 계속 전송됐다
(앱이 메모리의 `_sessionId` 를 localStorage 에 다시 써 넣는다). 앱의 ⋯ → '대화 새로 시작'
실제 경로로 리셋하자 **"어떤 고객님의 메모를 말씀하시는지 알려주세요"** 로 정확히 되물었다. 철회.

## 3. §14 테넌트 격리

| 경로 | 방법 | 결과 |
|---|---|---|
| 쓰기 6 kind 에 남의 샵 id 주입 | 격리 Postgres 실제 라우터 | **남의 잔액·매출·메모 변경 0** |
| 조회 엔드포인트 전체 | CI `tenant-isolation` 게이트(엔드포인트 자동추출 · SKIP 증가도 실패) — 이번 커밋마다 통과 | **PASS** |
| 라이브 교차 확인 | 계정이 user 4 ↔ 5 로 바뀌는 순간 잇비 답(16명/박지우 vs 21명/안원영)이 **바뀐 계정의 데이터와 일치** | **누출 0** |

### 오판할 뻔한 것 (P0 후보를 철회)
user 5 로 로그인돼 있는 줄 알았는데 잇비가 user 4 의 고객("E2E_A_박지우 — 4회")을 답해서
**테넌트 누출 P0** 로 적을 뻔했다. 같은 순간 `/auth/me` 를 다시 읽으니 **user 4** 였다 —
연준님이 그 사이 계정을 바꾼 것이다. 신원과 데이터를 **한 호출에서 같이** 읽고 나서 철회.
댓글 건(CASE-027)도 같은 방식으로 "user 5 본인 인스타 댓글" 임을 확인했다.

## 4. 이번 게이트에서 막은 데이터 오염 경로

| CASE | 무엇이 오염될 뻔했나 | 등급 |
|---|---|---|
| 025 | "단골 손님한테 감사 문자 써줘" → **'손님한테' 라는 고객 등록 카드** — 누르면 가짜 고객 행 | P1 · 수정·라이브 확인 |
| 017~019 | 748시간짜리 예약 → 그 달 예약 전부 거부 | P1 · 수정 |
| 021 | "오래 안 온 손님한테 문자" → "오래님을 못 찾았어요" (FE) | P1 · 수정 |

025 는 기존 불용어 **목록** 방식이 원인이었다. 조사(유한) + 집단명사(유한) **닫힌 집합**으로 바꿨고,
오탐 방지 테스트(실제 고객 이름 13종 — `아주아주긴이름의고객님` · `박지우 손님한테` · `고객사랑` 포함)를
같은 수만큼 넣었다.

## 5. 관측으로만 남기는 것 — 재현 안 됨

**"/auth/refresh 200 직후 /assistant/ask 401" 반복 (2026-09-12 08:17:52~53Z)**

```
ask 401 → refresh 200 → ask 401 → refresh 200 … (7회) → refresh 429
```

- FE 에는 single-flight 가 있다(`app-core.js` `_refreshing` + waiters) — FE 폭주 아님.
- 같은 시각 **연준님이 같은 Chrome 프로필에서 다른 계정으로 로그인·인스타 연동 중**이었다
  (localStorage 토큰 공유). 제 API 워커도 요청을 몰아 보내던 중이었다.
- 백엔드 `/auth/refresh` 는 로그아웃 경합을 막으려고 `iat_floor` 를 쓴다(2026-09-06 P1 수정).
- **원인을 특정하지 못했다.** 단독 세션에서 저속으로 재현을 시도하려 했으나 세션이 계속 바뀌어
  못 했다. 추측으로 결함 등급을 매기지 않는다 → **관측 기록만**, 별도 인증 감사에서 재현 필요.

## 판정

| 항목 | 결과 |
|---|---|
| 현재 빌드 로그 PII | **0** |
| 과거 로그 잔존 PII | 🟠 **21건(가상 이름)** — 삭제 방식 결정 필요 |
| facts 오염 | **0** |
| 테넌트 격리 | **침해 0** |
