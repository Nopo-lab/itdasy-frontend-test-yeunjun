============================================================
ITDASY — ZERO-HELP FIRST OWNER UX TRUE FINAL CLOSEOUT
============================================================

FINAL:
🔴 RED

LIVE FE:    75ac082 (라운드 중 d24d551 → e0c3f3e → 75ac082 로 다른 세션 배포가 이어짐)
LIVE BUILD: 20260912-0523-75ac082 (하드 새로고침 후 실행 번들 문자열로 확인)
LIVE BE:    itdasy-backend-staging-00591-rqj
ACCOUNT:    /auth/me 200 · id=4 · 테스트5.7
SW CACHE:   빌드와 일치하는 단일 캐시 (stale 없음)
기기:       Chrome(macOS) 606~1147px · iPhone 17 **시뮬레이터**(iOS 18_7 WebKit, 402×646, dpr 3)
            · Android **에뮬레이터** itdasy_galaxy(Android 10 Chrome, 411×762~786, dpr 2.625)
시작 슬롯:  mtu4ptpma7gvc · mtwaanxzx4nb6 · mtwaanxzx4nb6_conflict_1789107327563 (전부 내 것 아님)

P0: 0
P1: 0
P2: 0  ← 지난 라운드의 열린 P2 1건은 아래에서 **결함 아님**으로 닫음
P3: 1 (신규 · 토큰 자동갱신 구조) + 1 (기존 · work_memory 동의)
UNTESTED: 아래 목록
BLOCKED: 없음 (라운드 중 Chrome 확장 연결이 두 번 끊겼으나 복구됨)

------------------------------------------------------------
지난 라운드 열린 P2 — 판정: **결함 아님 (재현 안 됨)**
------------------------------------------------------------
의혹: "저장한 슬롯이 서버에도 없고 로컬에서도 사라졌다."

이번 라운드 실측:
1. 사진 3장 업로드 → 캡션 → 편집기에서 글자 → [완료]
2. 저장 직후: 로컬에 슬롯 생성 · **서버에는 아직 없음**
3. 약 10~20초 뒤 다시 조회: **서버에 올라와 있음** (`mtxxb1u1fp5uj`)
   강제 `WorkspaceSync.sync()` 전후가 동일 → 자동 push 가 정상 동작

즉 **정상 경로에서 초안은 로컬 저장 후 20초 안에 서버로 간다.**
지난 라운드의 소실은 그 20초 창에 **토큰 24시간 만료가 겹친 것**이다
(push 가 401 → `_handle401` → 토큰 삭제 → 로그아웃). 같은 조건을 만들지 못해
로컬 소실 단계는 재현하지 못했다. **결함으로 판정하지 않고 닫는다.**
다만 남는 위험은 아래 P3 와 같은 뿌리다.

------------------------------------------------------------
BASEURL / DOUBLE-BAKE: ✅ PASS (이번 라운드 1순위 항목)
------------------------------------------------------------
**LIVE 앱 · 사진 1장**
| 단계 | 결과 |
|---|---|
| 업로드 → `DOUBLE-BAKE-TEST` → 저장 | base `66947462` ≠ edited `fc6ea565` · output = edited |
| 앱 완전 새로고침 | 유지 (서버 왕복 후 base `ebbc64eb` / edited `35f8e239`) |
| 작업실 재진입 → 다시 편집 | **스테이지 배경 = `ebbc64eb` (= baseUrl)** · 구운 본 `35f8e239` 아님 |
| 텍스트 레이어 수 | **1** (중복 없음) |

**LIVE 앱 · 3장 캐러셀** (새로고침 뒤 재편집)
| 항목 | 결과 |
|---|---|
| 편집기 썸네일 | **3** (P2-1 수정이 새 빌드에서도 유지) |
| 스테이지 배경 | `ebbc64eb` = 원판 ✅ |
| 레이어 수 | 1 ✅ |
| B·C 에 글자 추가 후 저장 | photo0 `ebbc64eb`→`3e972952` · photo1 `aa1cbf3a`→`ab558a32` · photo2 `12bbc8b4`→`dbfe17fb` |
| 발행본 3장 | 각자 제 사진 + 제 글자, 글자 **1회씩** (눈으로 확인) |

**하네스(실엔진) 12회** — desktop/iPhone/Android 전부 `stageBgIsBaked=no`.

PASS 조건 5개 모두 충족: baseUrl=원판 / edited·output=편집본 / reopen 배경에 글자 없음 /
text layer 1개 / republish 이중 글자 없음.

⚠️ 관찰: 편집하지 않은 장은 `baseUrl === editedDataUrl` 이 된다.
이번엔 **편집한 장에서는 한 번도 일어나지 않았고**(위 6개 해시가 전부 다름) 이중 굽기도 없었다.
지난 라운드에 "잠재 위험"으로 적어둔 건 여기서 닫는다.

------------------------------------------------------------
OUTPUT INVARIANTS: ✅ PASS (6뷰포트 + 데스크톱 + 비율 3종)
------------------------------------------------------------
같은 편집(글자 1개, 4:5)을 폭만 바꿔 반복 — 저장→재편집→재저장까지.

| 뷰포트 | 스테이지 | 발행본 | 재편집 레이어 | 배경에 구움 | save1==save2 |
|---|---|---|---|---|---|
| 375×667 | 375×469 | **1080×1350** | 1 | no | ✅ |
| 390×844 | 390×488 | **1080×1350** | 1 | no | ✅ |
| 402×684 | 402×503 | **1080×1350** | 1 | no | ✅ |
| 430×932 | 430×538 | **1080×1350** | 1 | no | ✅ |
| 520×800 | 520×650 | **1080×1350** | 1 | no | ✅ |
| 768×1024 | 521×651 | **1080×1350** | 1 | no | ✅ |
| desktop 1024×768 | 614×768 | **1080×1350** | 1 | no | ✅ |

비율: 1:1 → **1080×1080** · 4:5 → **1080×1350** · 9:16 → **1080×1920** (전부 PASS)

🔑 스테이지가 375×469 ~ 614×768 로 1.6배 변해도 발행 해상도는 고정.
(2026-09-10 에 "창 크기가 발행 해상도를 결정"하던 결함이 재발하지 않았다는 회귀 증거)

------------------------------------------------------------
iPHONE: ✅ (시뮬레이터 — 실물 기기 아님)
------------------------------------------------------------
iPhone 17 **시뮬레이터** · iOS 18_7 실제 WebKit · 402×646 · dpr 3

실제 터치로 확인(헤어):
- 글자 도구 탭 → 레이어 생성 ✅
- **한글 IME 조합이 실제로 들어감** (ASCII 키가 한글 자판으로 조합돼 한글이 찍힘) ✅
- 보정 슬라이더 실제 드래그 → 밝기 **+21**, 사진이 눈에 띄게 밝아짐 ✅
- 스티커 시트 열림 — 추천/뷰티/귀여움/트렌디/아이콘/컬러 탭 + 뷰티 스티커 ✅
- [완료] → 편집기 정상 종료 ✅

엔진 왕복(사진 5종, 실 WebKit): 헤어·네일·속눈썹·전후(왁싱)·저조도
→ **5/5 VERDICT=PASS** (out 1080×1350 · save1==save2 · reopen layers=1 · 배경에 구움 없음)

❌ 못 한 것: 업로드 UI 경유(파일 선택기), 사진 채우기 토글, reload recovery

------------------------------------------------------------
ANDROID: ✅ (에뮬레이터 — 실물 기기 아님)
------------------------------------------------------------
itdasy_galaxy **에뮬레이터** · Android 10 Chrome · 411×762~786 · dpr 2.625

실제 터치로 확인(헤어):
- 소프트 키보드 열림 + **키보드 위로 편집 중인 글자가 계속 보임** ✅
- 텍스트 입력 `ANDROID-HAIR` ✅
- **시스템 백 = 키보드만 닫힘**, 편집기·레이어 유지 ✅
- 레이어 **터치 드래그로 이동** ✅
- 크기 슬라이더 드래그로 확대/축소 ✅
- 하단 패널이 제스처 바에 안 가림 (safe-area) ✅
- [완료] → `SAVED out=1080x1350 · layers=[ANDROID-HAIR] · ratio 4:5` ✅

엔진 왕복(헤어·네일·속눈썹): **3/3 VERDICT=PASS**

❌ 못 한 것: **핀치(두 손가락)** — `adb input` 에 멀티터치가 없어 미실행.
   크기 조절은 슬라이더로만 확인했다. 엣지 드래그(가장자리 스와이프백)도 미실행.

------------------------------------------------------------
P3-A 패널 obstruction: ✅ 재현 안 됨 (오탐 회피)
------------------------------------------------------------
iPhone 에서 "보정 패널이 열린 채 스티커 아이콘이 3번 무반응" 을 보고 결함을 의심했으나,
**히트테스트로 반증**했다 — 402×874 와 402×684 둘 다, 패널 **열림/닫힘 무관하게**
오른쪽 레일 버튼 5개가 전부 `elementFromPoint === 자기 자신`(5/5 SELF).
실제로 패널을 닫고 같은 좌표를 탭하니 스티커 시트가 **한 번에** 열렸다.
→ 내 좌표 오차였다. **결함으로 올리지 않는다.**

------------------------------------------------------------
🟡 P3 (신규) — 토큰 자동 갱신이 구조상 한 번도 못 돈다
------------------------------------------------------------
- BE `utils/security.py`: 액세스 토큰 **24시간**. 주석은 "FE 의 _tryRefresh 자동 갱신과 결합".
- BE `/auth/refresh`: docstring "**만료 임박 시** 클라가 호출", 구현은 `Depends(get_current_user)`
  → **유효한 토큰이어야** 새 토큰을 준다.
- FE `app-core.js`: `_tryRefresh()` 호출부는 **401 핸들러 한 곳뿐**(line 1564). 선제 갱신 타이머 없음.

⇒ 401 이 뜬 시점엔 이미 만료라 refresh 가 거절된다. **24시간마다 무조건 강제 로그아웃.**
리프레시 토큰이 따로 없어 슬라이딩 세션이 구조적으로 불가능하다.
지난 라운드가 이 경계에 걸려 작업 중 로그아웃됐다.

------------------------------------------------------------
🟡 P3 (기존) — work_memory 동의 (코드 확인만, 라이브 미검증)
------------------------------------------------------------
- `WorkMemory.autoOn()` 기본값 **true** · `ITDASY_WORK_MEMORY` 기본 **true**
- `setAutoOn` 은 export 돼 있으나 **호출하는 UI 가 없다** (grep 0건) → 끄는 스위치 없음
- 안내는 적용 시점의 배너 하나 — "평소처럼 만들었어요 / 마음에 안 들면 바로 빼드려요 [되돌리기]",
  **5초 뒤 사라지고 그 뒤엔 되돌리기도 무시**된다. 저장(학습) 시점 동의는 없다.
❌ **다음 업종 사진으로 오염되는지**는 이번에도 실행하지 못했다.

------------------------------------------------------------
S1-S20: 🔴 부분 (전수 아님)
------------------------------------------------------------
| 시나리오 | 상태 |
|---|---|
| S2 네일 색 살리기 | ✅ 채도 +54 실측, 발행본 반영 |
| S4 텍스트 한 줄 | ✅ 데스크톱·iPhone·Android 전부 |
| S9/S10 계열 히트테스트 | ✅ 레일 5/5 (P3-A 항목) |
| S13 재편집 | ✅ 저장→나가기→재진입→글자 수정→저장 (LIVE) |
| S16 375px | ✅ 출력 불변식 스윕으로 |
| S17 iPhone | ✅ 시뮬레이터 |
| S18 Android | ✅ 에뮬레이터 |
| S19 한글 IME | 🟡 iPhone 에서 **조합 입력은 확인**, 저장→reload→reopen 왕복은 미확인 |
| S1·S3·S5·S6·S7·S8·S11·S12·S14·S15·S20 | ❌ UNTESTED |

S20 FRESH RUNS:      ❌ 0/5 UNTESTED
ITBI:                ❌ UNTESTED (사진편집 자연어 연결 여부 확인 자체를 못 함)
SAVE FAILURE MATRIX: ❌ 14종 전부 UNTESTED
P3-B 작은 레이어:     ❌ UNTESTED
P3-C 캡션 이미지 밀림: ❌ UNTESTED (원인은 특정돼 있음 — hero `<img>` 에 width/height/aspect-ratio 없음.
                        되돌린 `aspect-ratio:4/5` 는 재적용 금지. 올바른 수정은 실제 출력 치수 기록 후 주입)
P3-E sync 죽음 신호:  ❌ UNTESTED
BEAUTY PHOTO QUALITY: ❌ UNTESTED (halo·클리핑·색 틀어짐 등 미측정)

------------------------------------------------------------
BUGS FOUND / FIXED
------------------------------------------------------------
BUGS FOUND: P0 0 · P1 0 · P2 0 · P3 2 (위 두 건)
BUGS FIXED: 없음 — 이번은 검증 라운드였고, 수정이 필요한 P2 이상이 나오지 않았다.
철회한 의심 2건: ① 캐러셀 B·C 편집 소실(확장 연결 끊김이 원인, 재실행 시 정상)
               ② iPhone 패널 obstruction(내 좌표 오차, 히트테스트로 반증)

TEST DATA CLEANUP: ✅ 완료
- 내가 만든 슬롯 `mtxt00fny5h7v`(지난 라운드) · `mtxxb1u1fp5uj`(이번) 둘 다 없음
- 현재 로컬/서버 모두 기준 3개만: mtu4ptpma7gvc · mtwaanxzx4nb6 · …_conflict_1789107327563
- `mtu4ptpma7gvc` 의 `LIVE1080` **건드리지 않음**
- QA 하네스(`_pe.html`·`qa_photos/`)는 워크트리 안에만 있고 **커밋·배포하지 않았다**

------------------------------------------------------------
FINAL OWNER ANSWER:
NOT PROVEN
============================================================
이유: P0/P1/P2 = 0 이고 이번 라운드에 이중굽기·출력 불변식·iPhone·Android 는 실제로 닫았다.
그러나 GREEN 은 `UNTESTED = 0` 을 요구하는데, S20 신규 원장 5회 · 저장 실패 매트릭스 14종 ·
잇비 · S1~S20 전수 · 뷰티 사진 품질이 남아 있다.
