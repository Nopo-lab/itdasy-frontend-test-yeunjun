# ITDASY — ABSOLUTE FINAL DEVICE GAP CLOSEOUT (2026-09-12)

브랜치 `feat/responsive-alldevice` · 워크트리 `../itdasy-wt-responsive` · 커밋 `31dc2c0`
**미배포 · main 머지 승인 대기**

이번 라운드는 **이미 PASS 한 항목을 다시 돌리지 않았다.** 직전까지 `NOT TESTED` 로
남아 있던 15개 갭만 닫았다.

---

## 0. 먼저 — 이번에도 못 한 것 (거짓말하지 않기 위해 맨 위에 둔다)

| 항목 | 상태 | 이유 |
|---|---|---|
| 실물 iPhone | 🔴 **NOT TESTED** | 이 맥에 연결된 물리 기기가 없다 |
| 실물 Galaxy / Android | 🔴 **NOT TESTED** | 〃 |
| 실물 iPad | 🔴 **NOT TESTED** | 〃 |
| 실제 사람 손가락 터치 정확도 (§16) | 🔴 **NOT TESTED** | 사람이 눌러야 측정되는 항목이다 |
| Samsung Internet | 🔴 **NOT TESTED** | 정식 설치 경로가 Play 스토어 로그인뿐이고, 계정 로그인·APK 사이드로딩은 내가 하면 안 되는 행위다 |
| iPad Split View / Stage Manager | 🔴 **NOT TESTED** | 시뮬레이터에서 두 번째 앱을 붙여 창을 나누는 조작을 수행하지 못했다. 다만 Split View 로 생기는 폭 대역(320~510pt)은 320~3440 폭 스윕과 600×808 실기기 감사로 덮여 있다 |

**"모든 스마트폰 모델 100% 테스트" 라고 주장하지 않는다.** 아래 §9 의 device class 기준으로만 말한다.

---

## 1. 이번 라운드 필수 숫자

```
PHYSICAL_IPHONE_TESTS        0   (NOT TESTED)
PHYSICAL_ANDROID_TESTS       0   (NOT TESTED)
PHYSICAL_IPAD_TESTS          0   (NOT TESTED)

ANDROID_TABLET_TESTS        45   화면감사 (3 구성 × 15 화면) / 히트테스트 11,995점
SAMSUNG_INTERNET_TESTS       0   (NOT TESTED)
DESKTOP_SAFARI_TESTS        75   화면감사 (5 뷰포트 × 15 화면) / 히트테스트 21,290점
IPHONE_PWA_LANDSCAPE_TESTS  15   화면감사 + 좌/우 safe-area 전용 프로브 16건
IPAD_KEYBOARD_TESTS          6   (하드웨어 키보드 / 소프트 키보드 / 입력 / 필터 / 가림 / 닫기)
IPAD_GESTURE_TESTS           5   (홈 / 앱전환 / 복귀 / 회전 / Safari 뒤로)

TEXT_SCALE_TESTS             7   iOS Dynamic Type 2 + Android font_scale 5 (1.0↔1.3 교차 대조 포함)
ZOOM_TESTS                  10   실제 Safari 줌 5단 + Blink 줌 5단

NETWORK_FAILURE_TESTS       94   (8 mutation × 10 장애조건 + 인스타 해제 8 + 중복전송 6)
NETWORK_RETRY_TESTS         72   (장애 해제 후 같은 버튼 재시도)
SLOW_CPU_TESTS              18   (9 화면 × 4× / 6× 스로틀, 애니메이션 중 난타·뒤로·회전)

LONG_PRESS_TESTS           212
BACKGROUND_RESUME_TESTS     14   (iPad 1 + Android 1 + 탭 강제종료 재시작 12)

TOTAL_INTERACTIONS      ≈ 37,200   (히트테스트 36,565 + 롱프레스 212 + 네트워크 탭 ~250
                                    + 스로틀 난타 ~110 + 키보드·제스처 ~40)
WRONG_TARGET                 0
BLOCKED_TARGET               0
DUPLICATE_ACTION             1   ← 고객센터 문의 전송 (§5-3, 미수정·근거 기록)
SAFE_AREA_COLLISION          0
LAYOUT_FAILURE               0
JS_ERROR                     0

P0                           0
P1                           0
P2                           2   (①시트 재열기 레이스 — 수정함  ②영문 예외문구 노출 — 미수정)
NOT_TESTED                   6   (§0 표)
```

---

## 2. device class 별 실측 표

전부 **실제 엔진**(real WebKit / real Blink)에서 돌렸다. 에뮬레이션 수치 아님.

| 구성 | 엔진 | 실측 뷰포트 | 화면 | 인벤토리 | 히트테스트 | WRONG | BLOCK | 겹침 | safe-area | JS에러 |
|---|---|---|---|---|---|---|---|---|---|---|
| Desktop Safari | WebKit 605.1.15 / Safari 26.6 | 1024×768 | 15 | 3,689 | 3,925 | 0 | 0 | 0 | 0 | 0 |
| Desktop Safari | 〃 | 1280×800 | 15 | 3,704 | 4,410 | 0 | 0 | 0 | 0 | 0 |
| Desktop Safari | 〃 | 1366×768 | 15 | 3,703 | 4,230 | 0 | 0 | 0 | 0 | 0 |
| Desktop Safari | 〃 | 1440×900 | 15 | 3,703 | 4,315 | 0 | 0 | 0 | 0 | 0 |
| Desktop Safari | 〃 | 1920×960¹ | 15 | 3,704 | 4,410 | 0 | 0 | 0 | 0 | 0 |
| Android Tablet | Chrome 124 (Android 15, Pixel Tablet AVD) | 1280×648 (가로) | 15 | 3,731 | 2,965 | 0 | 0 | 0 | 0 | 0 |
| Android Tablet | 〃 | 800×1128 (세로) | 15 | 3,696 | 5,080 | 0 | 0 | 0 | 0 | 0 |
| Android Tablet | 〃 | 600×808 (소형) | 15 | 3,696 | 3,950 | 0 | 0 | 0 | 0 | 0 |
| iPhone PWA 가로 | WebKit (iOS 26.4 시뮬, standalone) | 874×402 | 15 | 3,683 | 3,280 | 0 | 0 | 0 | 0 | 0 |
| **합계** | | | **135** | **33,309** | **36,565** | **0** | **0** | **0** | **0** | **0** |

¹ 이 맥의 디스플레이가 1920×1080 이라 브라우저 크롬을 빼면 뷰포트 높이는 960 이 최대다.
  **1920×1080 "뷰포트" 는 이 장비에서 물리적으로 만들 수 없다** — 폭만 1920 을 맞췄다.

---

## 3. iPhone PWA Landscape (§3) — 최초 실측

홈 화면에 **웹 앱으로 설치**(공유 ▸ 홈 화면에 추가 ▸ 웹 앱으로 열기 ON)한 뒤 측정.

| 항목 | 세로 | 가로 |
|---|---|---|
| `innerWidth × innerHeight` | 402×812 | **874×402** |
| `visualViewport` | 402×812 | 874×402 |
| `visualViewport.offset` | 0,0 | 0,0 |
| `navigator.standalone` | true | true |
| `display-mode: standalone` | true | true |
| `env(safe-area-inset-top)` | 0px | 0px |
| `env(safe-area-inset-right)` | 0px | **62px** |
| `env(safe-area-inset-bottom)` | **34px** | 20px |
| `env(safe-area-inset-left)` | 0px | **62px** |
| `screen.orientation` | portrait-primary | landscape-primary |
| PC 셸(`width≥768 and height≥600`) | false | **false** ✅ |
| 하단 탭바 — 화면 바닥까지 여백 | 48px (> 34) | 34px (> 20) |
| FAB 하단 여백 | 54px | 40px |
| 가로 넘침 | 0 | 0 |

🔑 **가로 874px 에서 PC 셸이 안 뜬다 = 1차 라운드의 `height >= 600px` 게이트가 실제 PWA 가로에서도 정확히 동작한다.**
   게이트가 없었다면 아이폰을 눕히는 순간 사이드바 + PC 헤더가 402px 높이를 다 먹었을 것이다.

### 좌/우 safe-area 62px 침범 — 전용 프로브 16화면

기존 감사기는 **하단 inset 만** 본다(코드 확인). 가로 PWA 는 좌우 62px 이 생기므로 전용 프로브를 새로 짰다.

- 숫자상 걸린 것: `.shop-drawer`(스크림, left=0/-320) · `drawer-close`(닫힌 드로어 내부, left=-54) · `잇데이 Pro` 배지(right=854 vs 한계 812)
- **전부 확인 결과 결함 아님**:
  - 드로어 2건 = **닫힌 채 화면 밖에 주차된 요소**와 **스크림**. 실제 패널은 다이나믹 아일랜드 오른쪽에서 시작한다(스크린샷 대조).
  - `잇데이 Pro` = 우측 62px 밴드 안에 들어가지만 **실제로 잘리지 않는다**. 우상단 코너를 확대해 확인함(오른쪽 여백 ~18px 확보, 코너 곡선에 닿지 않음). iOS 가 가로에서 좌우를 **대칭으로** 보고하는 값이고, 실제 장애물(아일랜드)은 한쪽에만 있다.
- **SAFE_AREA_COLLISION = 0**

---

## 4. iPad (§7) — 키보드 · 시스템 제스처

기기: iPad Air 11" (M4) 시뮬레이터, iOS 26.4, Safari.

### 키보드

| 상태 | 뷰포트 | visualViewport | 키보드 높이 | PC 셸 유지 | 입력창 가림 | 사이드바 | 가로 넘침 |
|---|---|---|---|---|---|---|---|
| 기본 | 820×1094 | 1094 | — | true | — | 232px | 0 |
| 하드웨어 키보드 연결 | 820×1094 | 1023 | **71px**(액세서리 바만) | true | 없음 | 232px | 0 |
| 소프트 키보드 | 820×1094 | 757 | **337px** | **true** ✅ | 없음 | 232px | 0 |

- focus → 실제 타이핑(`010-1001`) → **목록이 1행으로 정확히 필터됨**(김민지)
- 키보드가 떠 있는 동안 `position:fixed/sticky` 조작요소 중 **가려진 것 0개**
- 🔑 **키보드가 미디어쿼리를 흔들지 않는다** — `innerHeight` 는 1094 그대로, `visualViewport` 만 줄어든다.
  `(height >= 600px)` 게이트를 쓰는 이 앱에서 이건 반드시 확인해야 하는 항목이었다.
- 하드웨어 키보드 연결 상태(= 매직 키보드 쓰는 실제 아이패드 사용자)도 따로 측정했다.

### 시스템 제스처 · 백그라운드 복귀

| 조작 | 결과 |
|---|---|
| 홈(앱 나가기) → 25초 → Safari 복귀 | **JS 컨텍스트 생존**(표식 `__resume` 유지 = 리로드 안 됨) |
| 복귀 후 hash | `#customers` 유지 |
| 복귀 후 입력값 | `010-1001` 유지 |
| 복귀 후 필터 결과 | 1행 유지 |
| 고객 시트 | 열린 채 유지 · **중복 시트 0** |
| JS 에러 | 0 |
| 회전(세로 820×1094 → 가로 1180×734) | PC 셸 유지 · 사이드바 232 · 시트 오프셋 정확히 232(잘림 0) · 인덱스바 정상 · **검색어 유지** · 가로 넘침 0 · JS 에러 0 |
| Safari 좌측 엣지 뒤로 제스처 | ⚠️ **판정 불가** — 제스처가 전달되지 않아 hash/화면 변화 없음. 앱 결함인지 시뮬레이터 제약인지 가르지 못했다 → `INCONCLUSIVE` |

---

## 5. 네트워크 파괴검증 (§11)

목 API 에 **장애 주입 계층**을 새로 붙였다(`qa_fault.js`): 지연 / 끊김 / 무응답 / 429 / 500 / 503.
모든 조작은 **실제 좌표 터치**이고, 휠·피커도 실제 스와이프로 돌렸다.

### 5-1. 결과표 (각 8행 = 정상·지연 500ms·2s·5s·15s·끊김·무응답·429·500·503)

| mutation | 엔드포인트 | 지연 중 연타 3회 → 요청 수 | 즉시실패 후 재시도 | 영구 disabled | 스피너 잔존 | 실패인데 성공 토스트 | 판정 |
|---|---|---|---|---|---|---|---|
| 고객 저장 | `POST /customers` | **1** | 정상 복구 | 없음 | 없음 | 없음 | ✅ |
| 예약 저장 | `POST /bookings` | **1** | 정상 복구 | 없음 | 없음 | 없음 | ✅ |
| 매출 저장 | `POST /revenue` | **1** | 정상 복구 | 없음 | 없음 | 없음 | ✅ |
| 회원권 충전 | `POST /memberships/topup` | **1** | 정상 복구 | 없음 | 없음 | 없음 | ✅ |
| 회원권 차감 | `POST /memberships/use` | **1** | 정상 복구 | 없음 | 없음 | 없음 | ✅ |
| DM 설정 저장 | `PUT /shop/dm-menu` | **1** | 정상 복구 | 없음 | 없음 | 없음 | ✅ |
| 잇비 전송 | `POST /assistant/ask` | **1** | 정상 복구 | 없음 | 없음 | 없음 | ✅ |
| 고객센터 전송 | `POST /support/messages` | **1** | 정상 복구 | 없음 | 없음 | 없음 | ⚠️ §5-3 |
| 인스타 연결 해제 | `POST /instagram/disconnect` | **1** | 정상 복구 | 없음 | 없음 | 없음 | ✅ |
| 인스타 연결/재연결 | `POST /instagram/go-ticket` | — | — | — | — | — | ⛔ 외부 OAuth 이동이라 파괴검증 대상 아님 |

### 5-2. 특히 잘 되어 있던 것

- **오프라인 큐가 실제로 복구된다** — 끊긴 상태에서 고객 저장 → `itdasy_customers_offline_v1` 로컬 저장 →
  연결 복구 2.5초 안에 `POST /customers 200` 자동 발사 → `"오프라인에 저장했던 손님 1명을 올렸어요"`. 실측 확인.
- **회원권은 멱등키로 중복을 흡수한다** — 끊김에서 요청이 4회 나가지만 `client_txn_id` 를 재사용하고,
  토스트도 `"처리 결과를 확인하고 있어요. 같은 버튼을 다시 눌러도 두 번 처리되지 않아요"` 로 정직하다.
- **타임아웃을 '실패' 라고 단정하지 않는다** — 매출 저장 무응답 시 `"저장 결과를 확인하지 못했어요. 목록을 새로고침해 확인해 주세요"`.
- **재시도 금지 목록이 촘촘하다** — `app-core.js` 의 `NO_RETRY_PATH_RE` + `CREATE_NO_RETRY_RE` 가
  예약·매출·고객 생성, 인스타 발행, 공개 답글, 결제를 전부 막고 있다(코드 확인).

### 5-3. ⚠️ DUPLICATE_ACTION = 1 — 고객센터 문의 전송

**재현(단발 1탭 기준):**

| 장애 | 1탭 → 실제 POST 수 |
|---|---|
| 지연 3초 | 1 ✅ |
| 무응답(타임아웃) | **3** (ABORTED, ABORTED, 진행중) |
| 네트워크 끊김 | **4** (전부 NETERR) |

`app-core.js` 의 자동 재시도 래퍼가 `support/messages` 에는 안 걸려 있다.
`support/admin/reply`(관리자→원장 답장)는 금지 목록에 있는데 **원장→관리자 문의는 빠져 있다.**

- **끊김(NETERR)** 은 서버에 안 닿은 게 확실하므로 재시도가 안전하다.
- **무응답(타임아웃)** 은 "안 갔다" 가 아니라 **"모른다"** 다 → 문의가 2~3개 중복 등록될 수 있다.

**등급 P2 · 이번 라운드에서 고치지 않았다.** 돈·손님 피드에 나가는 경로가 아니고(운영자 문의함),
멱등키가 없는 대신 사용자에게 보이는 피해가 "같은 문의가 여러 번 보임" 에 그친다.
고치려면 `NO_RETRY_PATH_RE` 에 `support/messages(?!/read)` 한 줄이면 되지만,
**이 라운드의 원칙(재현한 것만, 그리고 GREEN 만들려고 손대지 않기)** 에 따라 보고만 하고 판단은 보스에게 넘긴다.

### 5-4. ⚠️ P2 — 영문 예외 문구·원시 JSON 이 원장님 토스트에 그대로

실측 문자열:

```
저장 실패: Failed to fetch
저장 실패: The user aborted a request.            ← DM 설정 저장
해제 실패: Failed to fetch
해제 실패: 해제 실패 (HTTP 500) {"detail":"server error"}   ← 인스타 연결 해제 ("해제 실패" 가 두 번 + 원시 JSON)
```

동작에는 문제가 없다(중복 0, 재시도 정상). **문구만의 문제**라 P2. 미수정.

### 5-5. 오해였던 것 2건 (계측이 만든 가짜 버그)

1. **"무응답이면 버튼이 영구 disabled"** → 내 장애 주입기가 `AbortController` 를 무시해서
   **앱이 스스로 끊는 걸 내가 막고 있었다.** signal 을 존중하도록 고치니 전부 정상 종료됐다.
2. **"예약 저장 버튼이 30초 뒤에도 비활성"** → 전체 요청을 15초 지연시킨 상태였고,
   저장 성공(25초) 뒤 **목록 새로고침 왕복까지 기다리는 것**이었다. 30초에 정상 종료. 결함 아님.
   (이중 저장 방지 관점에선 오히려 옳은 동작)

---

## 6. 느린 기기 / CPU 스로틀 (§12)

CPU **4× · 6×** 스로틀에서 9개 화면에 대해: 열기 직후 중앙 6연타 → 뒤로 → 재열기 → 뒤로 → 재열기 → 회전(가로↔세로).

| 항목 | 4× | 6× |
|---|---|---|
| 오버레이 중복 | 0 | 0 |
| 클릭스루(중앙이 엉뚱한 걸 집음) | 0 | 0 |
| 스피너 영구 잔존 | 0 | 0 |
| disabled stuck | 0¹ | 0¹ |
| JS 에러 | 0 | 0 |

¹ `매출 ›`(다음달 — 미래라 비활성), `현재 이용 중인 플랜입니다`(현재 플랜) 2건은 **정상적인 비활성**이다.

---

## 7. 🔧 이번 라운드에서 실제로 재현해서 고친 것

### P2-① 빠른 뒤로 뒤에 시트가 안 열린다 — `SheetAnim` 타이머 레이스 (수정함)

**재현(2/2 결정적):** `샵 관리` 열기 → **0.2초 안에** 뒤로 → **0.15초 안에** 다시 열기
→ 그 뒤로 눌러도 안 열린다. 화면엔 아무것도 없는데 `location.hash === '#settingsHub'`.

**원인:** `SheetAnim.close()` 가 220ms 뒤 `display:none` 을 거는 `setTimeout` 을 **취소하지 않는다.**
막 다시 연 시트를 그 타이머가 덮는다.

**2차 피해:** `_markSheetOpen('settingsHub')` 이 찍혀 있으므로 **다음 뒤로가기 한 번이 통째로 먹힌다.**
(코드 주석이 스스로 경고하던 "눌러도 아무 일 없는 뒤로가기" 가 여기서 생긴다.)

**영향 범위:** `SheetAnim` 을 쓰는 시트 전체. 실측으로 재현된 화면은 **샵 관리 · 연결된 서비스** 2개.
(예약·고객·매출·플랜·잇비·회원권·고객센터·DM·댓글·드로어 10개는 같은 조작에서 멀쩡 — 다른 open/close 경로)

**수정:** `open()` 에서 보류 중인 close 타이머를 취소. 재검증 — 200/150ms, 80/60ms 두 타이밍 모두 정상.

### P2-② 미니 달력 이전/다음 22×22 (§18 지시 — 구조 변경으로 해결)

- **1차(09-11)**: 22×22 에 투명 히트박스만 44 로 덮었다 → `다음` 이 `이전` 의 자기 영역을 먹어 **WRONG 발생** → 되돌림.
  둘 사이 `gap` 이 **2px** 뿐이었던 게 진짜 원인이었다.
- **2차(09-12)**: 보스 지시대로 **투명 히트박스 겹치기 말고 구조**로 갔다 —
  **버튼 36×36 + gap 2 → 10px**, 즉 **중심 간 거리 46px** 을 먼저 확보하고 그 위에 44×44 확장을 얹었다.
  좌측 컬럼 폭 예산: 36+10+36 = 82 + 월 라벨 ~72 = 154 ≤ 내부폭 180. 여유 있음.
  `.bk-mini` 는 PC 셸 전용이라 모바일 헤더에 파급 없음.
- **재검증(안드 태블릿 가로 1280×648, 손가락 기준)**: **WRONG 0 유지**, 44px 미달 목록에서 이전/다음 **사라짐**.

### 캐시 사고 직전 발견

`app-sheet-anim.js` 에 **`?v=` 가 아예 없었다.** 배포 자동 범프는 *이미 붙어 있는 것만* 갱신하므로,
위 수정이 옛 캐시 뒤에 숨어 그대로 안 나갈 뻔했다. 손으로 붙였다.
같은 상태인 스크립트가 `index.html` 에 **12개 이상** 더 있다 → 별도 작업으로 분리(이 라운드 범위 밖).

---

## 8. 텍스트 확대 (§9) · 실제 브라우저 줌 (§10)

### 시스템 글자 크기 — **이 앱에는 영향이 없다**

| 플랫폼 | 설정 | `html` | `body` | `h1` | 사이드 항목 | 가로 넘침 |
|---|---|---|---|---|---|---|
| iOS PWA | Dynamic Type 기본(large) | 16 | 15 | 19 | — | 0 |
| iOS PWA | **accessibility-XXXL** | 16 | 15 | 19 | — | 0 |
| Android Chrome | `font_scale 1.0` | 16 | 15 | 19 | 13.3 | 0 |
| Android Chrome | `font_scale 1.3` | 16 | 15 | 19 | 13.3 | 0 |

Android 는 **1.0 ↔ 1.3 을 2회 교차 대조**했다(같은 화면·같은 워밍업).
👉 **잘림·겹침·붕괴 0 — 단, 이유는 "잘 버텨서" 가 아니라 "시스템 글자 크기가 아예 안 먹어서" 다.**
   px 기반 타이포라 OS 설정을 따르지 않는다. **접근성 관점의 별개 과제**로 기록한다(이번 게이트 항목 아님).

⚠️ 중간에 "글자 크기 1.3 에서 잘림 6건" 이 떴는데, 대조군을 돌려보니 **1.0 에서도 똑같이 6건**이었다.
인라인 SVG 아이콘의 `scrollWidth-clientWidth` 아티팩트였다. 없는 버그를 만들 뻔했다.

### 실제 브라우저 줌 — 이게 이 앱을 실제로 확대하는 메커니즘

**실제 macOS Safari 에서 ⌘+/⌘- 로** 돌렸다(뷰포트 폭만 바꾸는 흉내 아님). 창 1440×900 기준.

| 줌 | CSS 뷰포트 | DPR | PC 셸 | 사이드바 | 탭바 | 가로 넘침 |
|---|---|---|---|---|---|---|
| 75% | 1920×1200 | 0.75 | ON | 260 | none | **0** |
| 100% | 1440×900 | 1 | ON | 260 | none | **0** |
| 125% | 1152×720 | 1.25 | ON | **232** | none | **0** |
| 150% | 960×600 | 1.5 | ON | 232 | none | **0** |
| 200% | 720×450 | 2 | **OFF** | 없음 | **grid** | **0** |

Blink(실제 Chrome) 80/100/125/150/200% 도 동일 결과. **두 엔진 결과가 완전히 일치한다.**

🔑 200% 에서 PC 셸이 **모바일 셸로 정상 강등**되고 탭바가 돌아온다 — 붕괴가 아니라 설계된 전환이다.
🔑 150% 에서 뷰포트 높이가 **정확히 600** 이 되어 `height >= 600px` 게이트 경계에 걸리는데, PC 셸을 유지한다.

---

## 9. 롱프레스 (§13)

10개 화면 × 화면 안에 보이는 조작요소 전부 = **212개**에 대해 800ms 롱프레스.

| 찾은 것 | 건수 |
|---|---|
| 원치 않는 컨텍스트 메뉴 | **0** |
| 원치 않는 텍스트 선택 | **0** |
| 드래그 시작(`dragstart`) | **0** |
| `draggable=true` 요소 | **0** |
| 롱프레스 전용 동작과 탭 동작이 충돌하는 요소 | **0** |

`user-select` 분포: `none` 176 / `auto` 36. `draggable` 전부 false.

⚠️ **정직하게 남길 한계 2가지**
1. `-webkit-touch-callout` 은 **Blink 에서 computed 값이 빈 문자열**이라 이 스윕으로는 판정 불가.
   iOS 의 "이미지 저장" / "링크 미리보기" 팝업 여부는 **WebKit 실측이 필요하다** → 이 항목만 `NOT TESTED`.
2. 샘플에 `<img>` 가 0개였다(선택한 10개 화면에 화면 안 이미지가 없었음). **작업실 갤러리 롱프레스는 미검증.**

---

## 10. 백그라운드 / 복귀 · 메모리 (§14, §15)

| 시나리오 | 엔진 | 결과 |
|---|---|---|
| 고객화면 + 검색어 + 시트 열림 → 홈 → 32초 → 복귀 | Android Chrome | 표식 생존 · hash·검색어·시트 유지 · **중복 시트 0** · JS 에러 0 |
| 고객화면 + 키보드 + 검색어 → 홈 → 25초 → 복귀 | iPad Safari | 표식 생존 · 전부 유지 · **중복 시트 0** · JS 에러 0 |
| **탭 강제종료 재현**(오버레이 열린 채 새로고침) | Chrome, 12개 화면 | **12/12 정상** |

탭 강제종료 재현 상세 — 12개 화면 전부에서:
- 유령 오버레이 **0**
- 남아서 클릭을 먹는 투명 스크림 **0**
- 깨진 hash **0** (새로고침 시 hash 를 비우고 유효한 탭으로 복귀)
- 불가능한 모달 상태 **0**
- JS 에러 **0**

---

## 11. 손가락 44px 재판정 (§17)

이번에 새로 감사한 6개 device class(안드 태블릿 3 · Desktop Safari 5 뷰포트 · PWA 가로)에서
44 미만으로 잡힌 항목을 **빈도 · 중요도 · 인접 충돌 · 대체 경로**로 다시 판정했다.

| 대상 | 실제 박스 | 유효 히트 | 오탭 | 판정 | 근거 |
|---|---|---|---|---|---|
| `.bk-mini__nav > button` (미니 달력 ‹ ›) | ~~22×22~~ → **36×36** | **44×44** | 0 | 🔧 **구조 수정함** | §7-② |
| `.bk-mini__day` (미니 달력 날짜칸) | 24×24 | 24×24 | 0 | **예외 유지** | 44 로 키우면 7칸×44+간격 = 320px 가 필요해 좌측 컬럼이 **본 달력을 잠식**한다. 같은 기능이 중앙 대형 달력에 훨씬 큰 타깃으로 있다. 자기 영역 5점 전부 자기에게 온다(정밀함, 작을 뿐) |
| `#bk-today-btn` (오늘) | 37×44 | 37×44 | 0 | **예외 유지** | 세로 44 충족, 가로만 7px 부족. 넓히면 PC·모바일 헤더 **양쪽**에 파급. 오탭 0 |
| `.bk-view__btn` (월 / 주) | 30×44 | 30×44 | 0 | **예외 유지** | 〃. 두 버튼 간 gap 1px 이라 히트박스 확장은 **금지**(같은 실수 반복 금지) |
| `.dmm-tg` (DM 켜기 토글) | 44×26 | 44×40 | 0 | **예외 유지** | 이웃 간격이 40 까지만 허용 — **잰 만큼만** 넓힌 상태 |
| `.ms-ic__seg-btn` (보통/크게) | 39×23 | 40×44 | 0 | **예외 유지** | 세그먼트 내부 칸막이라 가로 확장 불가 |
| `.legal-link` (이용약관 등) | 37×18 | 37×44 | 0 | **예외 유지** | 세로 44 확보. 가로는 글자 폭 그대로가 자연스럽다 |
| `#customerSheet .idx-bar span` (가나다) | 26×34 | 26×17 | 0 | **예외 유지** | 11글자를 세로로 꽉 채운 인덱스. 09-11 에 위치·겹침을 이미 고쳤고 오탭 0 |
| `.rv-pc__add` (+ 매출 입력) | 87×36 | 49×36 | 0 | **예외 유지** | 세로 36. PC 셸 전용이고 마우스·손가락 모두 오탭 0 |
| `#hv5CmsgRefresh` / `.hv5-itbi-*` 아이콘 | 28~34 | 44×23~28 | 0 | **예외 유지** | 홈 카드 헤더의 보조 아이콘. 주 동선에 큰 대체 버튼(`전체 보기 ›`) 있음 |

**"WRONG 0" 만으로 PASS 했다고 말하지 않는다.** 위 표가 판단 근거다.
다만 **실물 손가락으로 반복해서 못 누르는지는 사람이 눌러봐야 안다** → §0 의 `NOT TESTED` 로 남긴다.

---

## 12. §19 "전 기종" 최종 기준 판정

| device class | 상태 | 근거 |
|---|---|---|
| small iPhone | ✅ | 실기기 시뮬 WebKit (375×549 / 667×311) — 09-11 라운드 |
| normal iPhone | ✅ | 실기기 시뮬 WebKit (402×714 / 874×292) — 09-11 |
| large iPhone | ✅ | 실기기 시뮬 WebKit (440×796 / 956×330) — 09-11 |
| **iPhone PWA** | ✅ | **세로 402×812 + 가로 874×402 이번 라운드 실측** (이전엔 가로 NOT TESTED) |
| small Android | ✅ | Galaxy AVD 실기기 Chrome — 09-11 |
| normal Galaxy | ✅ | 411×786 / 842×331 실기기 Chrome — 09-11 |
| large Android | ✅ | 960×460 (density 320) 실기기 Chrome — 09-11 |
| **Samsung Internet** | 🔴 **NOT TESTED** | §0 |
| **Android Tablet** | ✅ | **1280×648 / 800×1128 / 600×808 이번 라운드 실측** |
| iPad | ✅ | 820×1094 / 1180×734 + 키보드 + 제스처 + 복귀 (이번 라운드 보강) |
| **Desktop Safari** | ✅ | **1024/1280/1366/1440/1920 폭 × 실제 WebKit 이번 라운드 실측** |
| Desktop Chrome | ✅ | 320~3440 폭 스윕 + 줌 5단 |

폭 스윕: **320px ~ 3440px** (960 지점) — 09-11 라운드 결과 유효.

→ Samsung Internet 1개가 비어 있으므로 **`SUPPORTED DEVICE CLASSES COMPLETE` 라고 선언하지 않는다.**

---

## 13. 최종 판정

```
P0 = 0                      ✅
P1 = 0                      ✅
WRONG_TARGET = 0            ✅
BLOCKED_TARGET = 0          ✅
SAFE_AREA_COLLISION = 0     ✅
LAYOUT_FAILURE = 0          ✅
JS_ERROR = 0                ✅
DUPLICATE_ACTION = 1        ❌ (고객센터 문의 전송, P2)

Physical iPhone             ❌ NOT TESTED
Physical Galaxy/Android     ❌ NOT TESTED
Physical iPad               ❌ NOT TESTED
Android Tablet              ✅ PASS
Samsung Internet            ❌ NOT TESTED
Desktop Safari              ✅ PASS
Text Scaling                ✅ PASS (붕괴 0 — 단 시스템 설정을 따르지 않음)
Network Failure / Retry     ✅ PASS (중복 1건 제외)
```

# 🟡 판정: GREEN 아님 — **ENGINE-COMPLETE / PHYSICAL-PENDING**

보스가 §21 에 적은 GREEN 조건은 **Physical iPhone·Galaxy·iPad = PASS** 와 **Samsung Internet = PASS**,
그리고 **DUPLICATE_ACTION = 0** 을 요구한다. 그중 4개가 충족되지 않았다.
**기준을 낮춰서 GREEN 이라고 쓰지 않는다.**

실엔진 기준으로는 이렇게 말할 수 있다:

> **실제 WebKit(iOS 시뮬 Safari·PWA·macOS Safari)과 실제 Blink(Android 에뮬 Chrome·Desktop Chrome)에서,
> 9개 구성 135화면 36,565점 히트테스트 · 롱프레스 212 · 네트워크 장애 94케이스 · CPU 6배 스로틀 ·
> 회전 · 키보드 · safe-area · 줌 200% · 백그라운드 복귀 · 탭 강제종료까지 수행해
> 엉뚱한 버튼 0 · 안 눌리는 버튼 0 · 레이아웃 붕괴 0 · JS 에러 0 을 실측했다.**

마지막 질문에 대한 답:

> *"…실제 사용자가 오류 없이 수행할 수 있는가?"*
>
> **실엔진 증거로는 YES. 실물 기기 증거로는 아직 대답할 수 없다.**
> Samsung Internet 은 Blink 계열이라 Chrome 결과가 시사하는 바는 있지만,
> **Chrome 결과를 Samsung Internet 결과로 대신하지 않는다.**

---

## 14. 보스가 결정해 줄 것

1. **머지 승인** — `feat/responsive-alldevice` → main. 아직 미배포다.
   (운영에는 아직 `height >= 600px` 게이트가 없어서 **갤럭시를 눕히면 데스크톱 UI 가 뜬다**)
2. **고객센터 문의 중복 전송(§5-3)** — `NO_RETRY_PATH_RE` 한 줄 추가할지 판단.
3. **영문 예외 문구 노출(§5-4)** — 별도 라운드로 뺄지 판단.
4. **실물 기기** — 아이폰 1대 + 갤럭시 1대 + 아이패드 1대를 맥에 붙여 주시면 §0 의 4개를 닫는다.
5. **Samsung Internet** — 갤럭시 실기기가 있으면 기본 브라우저로 바로 검증 가능하다.
